# Crypto Engine Hardening II

This tracker continues after the frozen crypto authorization, execution-admission, and crypto intelligence tracks. It keeps the same product boundary: Attestor is a governance and proof engine, not a wallet, custody platform, bundler, paymaster, bridge, facilitator, solver, relayer, oracle, screening vendor, or hosted crypto execution route.

## Guardrails

- Keep the core decision vocabulary stable: `admit`, `narrow`, `review`, and `block`.
- Preserve fail-closed behavior when policy, authority, evidence, freshness, enforcement, adapter readiness, or privacy posture is missing, stale, contradictory, or outside scope.
- Keep privacy sacred: no raw wallet metadata, raw transaction payloads, customer identifiers, custody callback bodies, provider error bodies, private policy thresholds, raw idempotency keys, payment headers, recipient details, or solver route secrets in telemetry, dashboards, proof packets, benchmarks, fixtures, or model-safe feedback.
- Customer-operated and third-party risk inputs remain digest-bound evidence. Attestor must not claim native sanctions, fraud, compliance, counterparty screening, market-data, bridge, solver, facilitator, wallet, custody, or hosted execution coverage.
- Prefer deterministic, inspectable intelligence over autonomous policy mutation.
- Every hardening PR must include a local probe or test that proves the behavior and the package boundary it changes.

## Research Anchors

Reviewed on 2026-05-11 before Steps 01-06:

- ERC-4337 keeps `UserOperation`, EntryPoint validation, bundler behavior, and simulation as pre-execution account-abstraction concerns. Adapter readiness must keep validation evidence explicit before handoff: [ERC-4337](https://eips.ethereum.org/EIPS/eip-4337)
- ERC-7562 constrains account-abstraction validation behavior and DoS-sensitive validation scope. A green adapter check should distinguish "simulation exists" from "validation posture is acceptable": [ERC-7562](https://eips.ethereum.org/EIPS/eip-7562)
- ERC-7579 defines modular smart-account execution through validator, executor, hook, and fallback-handler modules. Readiness must treat module and hook posture as first-class evidence: [ERC-7579](https://eips.ethereum.org/EIPS/eip-7579)
- EIP-7702 adds delegated EOA authorization and delegate-code posture. Delegated execution must not be treated as a generic wallet call: [EIP-7702](https://eips.ethereum.org/EIPS/eip-7702)
- x402 separates HTTP transport from payment requirement, verification, settlement, and response material. Agent-payment readiness must keep those evidence classes explicit and digest-bound: [x402 specification](https://github.com/x402-foundation/x402)
- Safe guards can reject transactions before or after execution. Guard readiness must include precheck, installation, recoverability, and terminal evidence posture rather than a boolean "Safe supported" label: [Safe Guards](https://docs.safe.global/advanced/smart-account-guards)
- Open Policy Agent decision logs preserve decision id, path, input/result shape, and masking controls. Pack-specific decision profiles should likewise expose traceable but model-safe decision facts rather than raw inputs: [OPA decision logs](https://www.openpolicyagent.org/docs/management-decision-logs)
- AWS IAM policy evaluation gives explicit deny precedence over allow and leaves missing allow as an implicit deny. Attestor policy intelligence follows the same safety shape: explicit deny is terminal, missing coverage fails closed, and conflicts require operator resolution: [AWS IAM policy evaluation logic](https://docs.aws.amazon.com/IAM/latest/UserGuide/reference_policies_evaluation-logic.html)
- Grafana dashboard guidance says dashboards should answer a concrete question and reduce cognitive load. Attestor proof console summaries should therefore expose ranked blockers, missing evidence, and readiness heatmaps instead of forcing operators to browse raw evidence: [Grafana dashboard best practices](https://grafana.com/docs/grafana/latest/visualizations/dashboards/build-dashboards/best-practices/)
- OpenTelemetry attribute limits exist because unbounded attributes can exhaust memory, and requirement levels account for cardinality and security. Attestor dashboard contracts should keep rows bounded and use low-cardinality statuses, reason codes, counts, and digests: [OpenTelemetry common specification](https://opentelemetry.io/docs/specs/otel/common/), [OpenTelemetry attribute requirement levels](https://opentelemetry.io/docs/specs/otel/common/attribute-requirement-level/)
- Node.js `perf_hooks` provides monotonic high-resolution measurement for local benchmarks, while Node's `crypto.createHash()` is the stable SHA-256 digest primitive used around canonicalized evidence. Runtime efficiency checks should therefore benchmark hot paths with warmup, aggregate samples, per-unit cost, and digest-bound outputs: [Node.js perf_hooks](https://nodejs.org/api/perf_hooks.html), [Node.js crypto](https://nodejs.org/api/crypto.html)
- Node.js package `exports` define which package subpaths are importable and encapsulate non-exported internals; TypeScript `moduleResolution: bundler` supports package `exports` resolution; npm documents `exports` as the public interface control for a package. Package-surface hardening should therefore compare the export map, public surface descriptors, and blocked deep-import probes instead of relying on docs alone: [Node.js packages](https://nodejs.org/api/packages.html#exports), [TypeScript moduleResolution](https://www.typescriptlang.org/tsconfig/#moduleResolution), [npm package.json exports](https://docs.npmjs.com/cli/v11/configuring-npm/package-json#exports)
- NIST Privacy Framework data-processing practices emphasize managed, limited, and protected data processing; OWASP logging guidance says logs should exclude secrets, access tokens, personal data, and other sensitive material; GDPR Article 5 includes data minimisation; OpenTelemetry cautions against sensitive and high-cardinality attributes. Crypto privacy enforcement should therefore reject both forbidden field names and sensitive material patterns before telemetry, dashboards, proof packets, benchmarks, or model-safe feedback are emitted: [NIST Privacy Framework](https://www.nist.gov/privacy-framework), [OWASP Logging Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Logging_Cheat_Sheet.html), [GDPR Article 5](https://eur-lex.europa.eu/legal-content/EN/TXT/?uri=CELEX%3A32016R0679), [OpenTelemetry attribute requirement levels](https://opentelemetry.io/docs/specs/otel/common/attribute-requirement-level/)
- NIST AI RMF separates risk governance, mapping, measuring, and managing. Attestor pack decisions should therefore produce inspectable profile artifacts that state posture, action, checks, and evidence gaps instead of opaque "smart" verdicts: [NIST AI RMF](https://www.nist.gov/itl/ai-risk-management-framework)
- `@noble/hashes` v2 is ESM-only, declares Node `>=20.19.0`, exposes `sha3.js` and `utils.js` through package `exports`, and rejects raw string input for hash functions. Runtime crypto dependency cleanup must therefore validate installed version, lockfile integrity, public `.js` subpath imports, and bytes-only Keccak vectors before accepting a major bump: [noble-hashes README](https://github.com/paulmillr/noble-hashes), [npm @noble/hashes](https://www.npmjs.com/package/@noble/hashes)
- Node.js 26 is `Current`, while production applications should only use Active LTS or Maintenance LTS releases. The official Docker Node images support active Node versions, but `node:alpine` uses musl libc, can expose `process.dlopen` shared-library gaps, and Node 26 images no longer bundle Yarn v1. Runtime-image major bumps must therefore be gated on project engine alignment, CI baseline alignment, Docker build plus API/worker runtime smoke, and native dependency checks before production acceptance: [Node.js releases](https://nodejs.org/en/about/previous-releases), [nodejs/docker-node](https://github.com/nodejs/docker-node)

## Step List

| Step | Status | Deliverable | Evidence | Notes |
|---|---|---|---|---|
| 01 | complete | Adapter readiness intelligence profile | `src/crypto-execution-admission/adapter-readiness-manifest.ts`, `tests/crypto-execution-admission-adapter-readiness-manifest.test.ts`, `tests/crypto-execution-admission-platform-surface.test.ts`, `tests/crypto-intelligence-platform-surface.test.ts`, `tests/crypto-authorization-core-intelligence-privacy-minimization.test.ts`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs` | Adds deterministic posture, readiness score, risk-factor, next-action, standards-coverage, attention-item, and digest-first privacy flags above the existing adapter readiness manifest. |
| 02 | complete | Pack-specific decision logic | `src/consequence-admission/pack-decision-profile.ts`, `tests/consequence-admission-pack-decision-profile.test.ts`, `src/consequence-admission/index.ts`, `src/consequence-admission/data-minimization-redaction-policy.ts`, `scripts/probe/probe-consequence-admission-package-surface.mjs` | Adds deterministic finance and crypto pack decision profiles over canonical admissions. The profile emits pack posture, recommended action, model-safe signals, check/proof summaries, and privacy-safe facts without changing the shared `admit` / `narrow` / `review` / `block` contract. |
| 03 | complete | Policy intelligence deepening II | `src/crypto-authorization-core/policy-gap-narrowing.ts`, `tests/crypto-authorization-core-policy-gap-narrowing.test.ts`, `tests/crypto-authorization-core-intelligence-privacy-minimization.test.ts`, `tests/crypto-authorization-core-platform-surface.test.ts`, `tests/crypto-intelligence-platform-surface.test.ts`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs` | Adds deterministic policy intelligence routing over coverage profiles: explicit deny dominates, conflicts require operator resolution, implicit deny and missing policy fail closed, stale evidence routes to refresh, and review-only coverage routes to review without leaking raw policy, wallet, customer, or threshold material. |
| 04 | complete | Proof console and dashboard hardening | `src/crypto-authorization-core/intelligence-dashboard-summary.ts`, `tests/crypto-authorization-core-intelligence-dashboard-summary.test.ts`, `tests/crypto-authorization-core-platform-surface.test.ts`, `tests/crypto-intelligence-platform-surface.test.ts`, `scripts/probe/probe-crypto-authorization-core-package-surface.mjs`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs` | Adds bounded proof-console priority widgets, ranked top blockers, policy-routing proof links, policy-routing failure rows, and readiness heatmap rows while keeping the dashboard decision-support-only and raw-payload drilldown disabled. |
| 05 | complete | Runtime performance and efficiency | `src/crypto-authorization-core/intelligence-performance-budget.ts`, `tests/crypto-authorization-core-intelligence-performance-budget.test.ts`, `scripts/benchmark-crypto-intelligence-performance.ts`, `tests/crypto-authorization-core-platform-surface.test.ts`, `tests/crypto-intelligence-platform-surface.test.ts`, `scripts/probe/probe-crypto-authorization-core-package-surface.mjs`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs` | Adds per-unit timing, benchmark regression profiles, default regression budgets, digest-key-only memoization boundaries, and fail-closed regression status without storing raw benchmark inputs or allowing raw payload caches. |
| 06 | complete | Package surface consistency | `src/crypto-intelligence/index.ts`, `tests/crypto-intelligence-package-surface-consistency.test.ts`, `tests/crypto-intelligence-platform-surface.test.ts`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs`, `tests/package-script-runner.test.ts` | Adds a digest-bound package-surface consistency profile that fails closed when crypto pack exports, surface descriptors, private package posture, or blocked deep-import probes drift across consequence admission, crypto authorization core, crypto execution admission, and crypto intelligence surfaces. |
| 07 | complete | Privacy and data-minimization enforcement II | `src/crypto-authorization-core/intelligence-privacy-minimization.ts`, `tests/crypto-authorization-core-intelligence-privacy-minimization.test.ts`, `scripts/probe/probe-crypto-authorization-core-package-surface.mjs`, `scripts/probe/probe-crypto-intelligence-package-surface.mjs` | Extends crypto privacy enforcement with raw prompt, model output, tool payload, provider body, wallet address, route detail, customer contact, idempotency, policy-threshold, recipient, API-key, webhook-secret, and customer-id leakage guards. The gate fails closed on forbidden fields and sensitive material patterns while keeping evaluation outputs digest-bound and raw-value-free. |
| 08 | complete | Dependency risk cleanup | `tests/crypto-authorization-core-noble-hashes-dependency-risk.test.ts`, `package.json`, `tests/package-script-runner.test.ts`, `tests/crypto-engine-hardening-ii-docs.test.ts` | Validates the already-landed `@noble/hashes` v2 dependency state separately from the closed Dependabot PR: lockfile version and integrity, ESM/package export posture, reviewed `sha3.js` runtime subpath, and bytes-only Keccak vectors are now guarded before future crypto dependency drift can pass unnoticed. |
| 09 | complete | Node 26 runtime validation | `tests/node-26-runtime-validation.test.ts`, `Dockerfile`, `package.json`, `SECURITY.md`, `.github/workflows/evaluation-smoke.yml`, `.github/workflows/security-scan.yml`, `.github/workflows/full-verify.yml`, `.github/workflows/release-provenance.yml`, `tests/package-script-runner.test.ts`, `tests/crypto-engine-hardening-ii-docs.test.ts` | Node 26 remains a no-go for the production Docker baseline until it reaches LTS, `package.json` engines are deliberately widened, reviewer/runtime CI runs the new baseline, Docker build plus API/worker runtime smoke passes, and Alpine/native dependency edges are revalidated. |
| 10 | blocked | Production rollout readiness | deployment target and operator-managed runtime access | Commercial billing configuration has repo-side bootstrap evidence, but full production rollout still requires operator-managed deployment env, service restart, readiness probe, and smoke tests on a working deployment target. |

## Current Posture

Steps 01-09 are implemented in code as reusable package surfaces, not just documentation. The remaining production rollout step is intentionally blocked on usable production infrastructure.

# Live Proof Register

This register tracks evidence that cannot be closed by repository changes
alone. A proof listed here remains `needs live test` or `needs ops proof` until
the live environment, cloud control plane, or customer integration produces
verifiable evidence.

Do not mark these items closed from documentation-only remediation.

## Proof Status Labels

- `required`: required before the named readiness stage.
- `repo-gated`: repository contains a check or flag requiring operator proof.
- `captured`: live evidence captured and linked.
- `blocked`: cannot run because a dependency is missing.
- `not applicable`: explicitly not required for the current phase.

## Current Live Proofs

| Proof ID | Readiness stage | Status | Required evidence | Repo gate / evidence | Current limitation |
|---|---|---|---|---|---|
| `LP-HTTPS-EDGE` | Live shadow | `repo-gated` | HTTPS listener or cloud load balancer TLS termination probe, including redirect/cleartext denial. | `scripts/check/check-ops-live-shadow-readiness.mjs`; `ATTESTOR_LIVE_SHADOW_HTTPS_PROOF`; OPS-01. | Active repo gateway may remain HTTP when TLS is proven by cloud LB. |
| `LP-CLUSTER-SECRET-STORE` | Live shadow | `repo-gated` | External Secrets `ClusterSecretStore` backend type, Workload Identity binding, and Secret Manager/Vault access audit. | `ATTESTOR_CLUSTER_SECRET_STORE_PROOF`; GKE example in ops provider docs; OPS-02. | Backend is environment-specific and not committed. |
| `LP-NETWORK-POLICY` | Live shadow | `repo-gated` | Applied default-deny and explicit allowlist NetworkPolicies verified in cluster. | `ATTESTOR_NETWORK_POLICY_PROOF`; `ops/kubernetes/ha/networkpolicy.yaml`; `ops/kubernetes/observability/networkpolicy.yaml`; OPS-04 and OPS-54. | CNI enforcement must be proven in the live cluster, including any managed observability backend egress override. |
| `LP-EDGE-WAF` | Live shadow | `repo-gated` | Cloud Armor, AWS WAFv2, or equivalent edge WAF policy attached with intended rules. | `ATTESTOR_EDGE_WAF_PROOF`; active GKE backend policy references Cloud Armor; AWS HTTPS example references WAFv2; OPS-05 and OPS-22. | Provider-specific WAF rules are outside the repo. |
| `LP-GCP-IAM-LEAST-PRIVILEGE` | Live shadow | `repo-gated` | GCP IAM role list for runtime, worker, External Secrets, and Cloud SQL access. | `ATTESTOR_GCP_IAM_LEAST_PRIVILEGE_PROOF`. | IAM policy is live cloud state. |
| `LP-RUNTIME-PKI-STORAGE` | Live shadow | `repo-gated` | Release runtime PKI StorageClass, encryption-at-rest, access mode, and backup/restore boundary proof. | `ATTESTOR_RELEASE_RUNTIME_PKI_STORAGE_PROOF`; PKI PVC annotations; OPS-17. | StorageClass and encryption are cluster facts. |
| `LP-KEYLESS-CA-PRODUCTION-GUARD` | Live shadow / limited enforcement | `repo-gated` | Production-like runtime proves no process-local keyless CA is auto-generated before release-runtime CA configuration, and the configured CA fingerprint remains stable across restart or rollout. | `tests/f5-keyless-ca-injection-boundary-validation.test.ts`; `src/signing/keyless-signer.ts` production-like fail-closed guard. | Repository tests prove the guard only; live deployment still must prove the operator-configured CA path and restart behavior without raw key material. |
| `LP-TLS-MATERIAL-SOURCE` | Live shadow | `repo-gated` | One selected TLS material path: cert-manager OR External Secrets, not both. | `ATTESTOR_TLS_MATERIAL_SOURCE_PROOF`; provider README contracts; OPS-18. | Operator must prove selected path in the live overlay. |
| `LP-BRANCH-PROTECTION-SIGNATURES` | Live shadow / enterprise pilot | `repo-gated` | GitHub branch protection for `master` shows `required_signatures.enabled == true`, and an unsigned-commit merge attempt is rejected. | `ATTESTOR_BRANCH_PROTECTION_SIGNATURES_PROOF`; `.github/workflows/branch-governance.yml` required-signatures API check with dedicated `ATTESTOR_BRANCH_GOVERNANCE_TOKEN` admin-read secret. | Repository workflow assertion does not prove the live GitHub setting or negative merge behavior until captured; the default `GITHUB_TOKEN` is not treated as admin-setting proof. |
| `LP-BRANCH-PROTECTION-REVIEWS` | Live shadow / enterprise pilot | `repo-gated` | GitHub branch protection for `master` shows required PR reviews, at least one approving review, CODEOWNER review, stale-review dismissal, and last-push approval enabled; same-author/self-merge bypass is rejected. | `ATTESTOR_BRANCH_PROTECTION_REVIEWS_PROOF`; `.github/workflows/branch-governance.yml` required-review API check with dedicated `ATTESTOR_BRANCH_GOVERNANCE_TOKEN` admin-read secret. | Repository workflow assertion does not prove the live GitHub setting or negative merge behavior until captured; the default `GITHUB_TOKEN` is not treated as admin-setting proof. |
| `LP-PUBLIC-ARTIFACT-REDACTION` | Public demo / marketing | `repo-gated` | Committed public artifact roots are scanned with the shared secret-safe redactor before publication. | `npm run check:public-artifacts-redaction`; `.github/workflows/security-scan.yml` public-artifact-redaction job. | Does not scan arbitrary local/live artifacts or generated files not committed under the configured public roots. |
| `LP-FINDING-TEST-COVERAGE` | Reviewer evidence / enterprise pilot | `repo-gated` | Current high-signal P0/P1 findings in `docs/audit/finding-index.md` have a machine-checked `Locking test:` marker when repo-closed, or an explicit future `Test contract:` when live/ops proof is still required. | `npm run test:audit-finding-test-coverage`; `.github/workflows/evaluation-smoke.yml` audit finding test coverage step. | This proves index discipline only; it does not prove every historical report row, live environment behavior, or production readiness. |
| `LP-SHARED-REPLAY-STORE` | Live shadow / limited enforcement | `repo-gated` | Multi-instance replay and nonce rejection across API replicas with Redis/Postgres backing. | `ATTESTOR_SHARED_REPLAY_STORE_PROOF`; baseline and token/replay posture; `tests/local-consequence-shared-store-rehearsal.test.ts` is local rehearsal evidence only. | In-memory fallback cannot prove HA replay safety; local embedded-PostgreSQL rehearsal does not prove deployed API replica routing. |
| `LP-SHARED-INTROSPECTION-STORE` | Limited enforcement | `repo-gated` | Introspection cache/store behavior under restart, outage, and stale-token scenarios. | `ATTESTOR_SHARED_INTROSPECTION_STORE_PROOF`; release-enforcement primitives and live ops gate. | Requires deployed shared backing services; local consequence retry/replay rehearsal is not introspection-store live proof. |
| `LP-POLICY-MUTATION-AUDIT-CHAIN-SHARED-STORE` | Limited enforcement | `repo-gated` | Live multi-instance deployment proves policy mutation audit log writes are shared-store backed, not file-backed only, and chain integrity is preserved across instances. | `ATTESTOR_POLICY_MUTATION_AUDIT_CHAIN_PROOF`; OPS-161 remediation; `tests/release-policy-control-plane-proof-discipline.test.ts`. | File-backed writer cannot prove multi-instance policy mutation audit integrity. |
| `LP-POLICY-ACTIVATION-APPROVAL-SHARED-STORE` | Limited enforcement | `repo-gated` | Live multi-instance deployment proves policy activation approval requests and decisions are shared-store backed, durable across API replicas, and preserve approval-gate outcomes after restart/failover. | `ATTESTOR_POLICY_ACTIVATION_APPROVAL_STORE_PROOF`; OPS-162 remediation; `tests/release-policy-control-plane-proof-discipline.test.ts`. | In-memory and file-backed approval stores cannot prove multi-instance approval durability. |
| `LP-RELEASE-DECISION-LOG-SHARED-STORE` | Limited enforcement | `repo-gated` | Live multi-instance deployment proves release decision log appends use the shared PostgreSQL release-authority store across API replicas, preserve contiguous sequence/hash-chain integrity, and fail closed on tampered persisted rows after restart/failover. | `ATTESTOR_RELEASE_DECISION_LOG_SHARED_STORE_PROOF`; OPS-149 remediation; `src/service/release-decision-log-store.ts`; `tests/release-decision-log-store.test.ts`; `tests/production-shared-request-path-cutover.test.ts`; `tests/production-shared-multi-instance-recovery.test.ts`. | Repository tests prove embedded-PostgreSQL shared-store behavior; external customer-operated PostgreSQL, network, credentials, backup, and rehearsal proof remain live environment facts. |
| `LP-CONSEQUENCE-RETRY-ATTEMPT-LEDGER-SHARED-STORE` | Live shadow / limited enforcement | `repo-gated` | Live multi-instance deployment proves consequence-admission retry-attempt records use the shared atomic store, enforce duplicate attempt/idempotency constraints across instances, and run with RLS forced for the target tenant scope. | `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF`; OPS-168 remediation; `tests/consequence-admission-proof-discipline.test.ts`; `tests/local-consequence-shared-store-rehearsal.test.ts` is local embedded-PostgreSQL rehearsal evidence only. | Repository schemas and local embedded-PostgreSQL rehearsal do not prove multi-instance deployed retry-budget enforcement, customer-operated database posture, or deployed RLS posture. |
| `LP-CUSTOMER-PEP-NO-BYPASS` | Limited enforcement | `repo-gated` | Reference customer PEP integration where direct downstream bypass fails. | `ATTESTOR_CUSTOMER_PEP_NO_BYPASS_PROOF`; baseline P0 blocker. | Repository middleware alone cannot prove customer deployment enforcement. |
| `LP-KMS-RUNTIME-SIGNING` | Limited enforcement / enterprise pilot | `repo-gated` | KMS/HSM-backed signing proof, key authority binding, and no raw private key export path. | `ATTESTOR_KMS_RUNTIME_SIGNING_PROOF`; baseline P0 blocker; PKI PVC boundary docs. | File/PVC signing remains insufficient for production authority claims. |
| `LP-DEGRADED-MODE-OUTAGE` | Limited enforcement | `repo-gated` | Provider/Redis/Vault/DB outage test proving fail-closed or bounded degraded behavior. | `ATTESTOR_DEGRADED_MODE_OUTAGE_PROOF`; degraded-mode repo hardening. | Static checks do not prove live outage behavior. |
| `LP-OBSERVABILITY-ALERT-DELIVERY` | Live shadow | `repo-gated` | End-to-end Alertmanager delivery to at least one warning and one critical receiver, plus Watchdog route validation. | `ATTESTOR_OBSERVABILITY_ALERT_DELIVERY_PROOF`; OPS-SWEEP-03 remediation; Alertmanager renderer/probe scripts. | Static receiver config and render tests do not prove an external pager/webhook receives alerts. |
| `LP-OBSERVABILITY-BACKEND-AUTH` | Live shadow | `repo-gated` | Loki/managed backend auth boundary, tenant header behavior, and namespace/network access proof. | `ATTESTOR_OBSERVABILITY_BACKEND_AUTH_PROOF`; local Loki auth enabled; OTel/Grafana `X-Scope-OrgID` contract. | Managed backend or live cluster auth behavior is environment-specific. |
| `LP-OBSERVABILITY-STORAGE` | Live shadow / limited enforcement | `repo-gated` | Loki/Tempo storage backend, encryption-at-rest, retention, and backup/restore proof. | `ATTESTOR_OBSERVABILITY_STORAGE_PROOF`; OPS-SWEEP-03 documentation and retention env wiring. | Local filesystem backends are evaluation/shadow rehearsal only. |
| `LP-BUDGET-ALERTING` | Live shadow | `repo-gated` | Cloud budget telemetry source and alert delivery proof. | `ATTESTOR_BUDGET_ALERTING_PROOF`; `AttestorBudgetTelemetryMissing` alert makes absence visible. | Real GCP billing budget integration is live cloud state. |
| `LP-POSTGRES-PITR-OFFSITE` | Live shadow / limited enforcement | `repo-gated` | PostgreSQL base backup, offsite WAL archive, checksum verification, encryption-at-rest, and restore-drill evidence. | `ATTESTOR_POSTGRES_PITR_OFFSITE_PROOF`; OPS-SWEEP-04 adds offsite WAL archive hooks and checksum verification. | Repository scripts do not prove offsite object-store durability or a successful live restore. |
| `LP-REDIS-AUTH-BOUNDARY` | Live shadow / limited enforcement | `repo-gated` | Redis ACL/password policy, network isolation, secret rotation, and replay/idempotency behavior under restart. | `ATTESTOR_REDIS_AUTH_BOUNDARY_PROOF`; OPS-SWEEP-04 hardens the DR reference config and compose path. | Managed Redis behavior and cluster reachability are live environment facts. |
| `LP-OBSERVABILITY-POD-SECURITY` | Live shadow | `repo-gated` | Pod Security Admission or equivalent runtime proof that collector securityContext constraints are enforced. | `ATTESTOR_OBSERVABILITY_POD_SECURITY_PROOF`; OPS-SWEEP-04 adds collector non-root/seccomp/no-privilege-escalation/read-only-rootfs manifest evidence; OPS-SWEEP-05 adds restricted Pod Security Admission namespace labels. | Node/runtime enforcement is not proven by YAML alone. |
| `LP-ADMIN-ACTOR-ROLE-SPLIT` | Live shadow / limited enforcement | `repo-gated` | Live deployment uses role-scoped admin keys for read, account, key, billing, ops, release, break-glass, and audit operators; mutation audit records show distinct actor labels and roles; legacy superuser key is rotated or tightly held. | `ATTESTOR_ADMIN_ACTOR_ROLE_SPLIT_PROOF`; OPS-SWEEP-06 adds route role allowlists, role-scoped admin credential support, and HTTP tests. | Repository role gates do not prove the deployed operator key material is split. |
| `LP-ADMIN-RATE-LIMIT` | Live shadow | `repo-gated` | Abuse probe against `/api/v1/admin/*` shows over-limit requests receive 429 before expensive admin credential comparison, and `AttestorAdminAuthFailure` alert behavior is visible. | `ATTESTOR_ADMIN_RATE_LIMIT_PROOF`; OPS-SWEEP-06 adds path-scoped admin auth rate limiting before `currentAdminAuthorized` and a HTTP regression test. | Static tests do not prove live edge/client IP behavior or alert delivery. |
| `LP-STRIPE-WEBHOOK-CONFIG` | Live shadow | `repo-gated` | `STRIPE_WEBHOOK_SECRET` is configured for the live Stripe dashboard endpoint; a fake-signature request to `/api/v1/billing/stripe/webhook` returns 400 without downstream mutation. | `ATTESTOR_STRIPE_WEBHOOK_PROOF`; OPS-SWEEP-08 maps the route and keeps Stripe SDK `constructEvent` signature verification. | Repository proof does not show real Stripe dashboard binding or live endpoint secret rotation. |
| `LP-SENDGRID-WEBHOOK-CONFIG` | Live shadow | `repo-gated` | `ATTESTOR_SENDGRID_EVENT_WEBHOOK_PUBLIC_KEY` is configured from the live Twilio SendGrid signed event webhook; fake-signature request to `/api/v1/email/sendgrid/webhook` returns 400. | `ATTESTOR_SENDGRID_EVENT_WEBHOOK_PROOF`; OPS-SWEEP-08 maps the route and adds direct verifier unit tests. | Repository proof does not show real Twilio SendGrid webhook binding. |
| `LP-MAILGUN-WEBHOOK-CONFIG` | Live shadow | `repo-gated` | `ATTESTOR_MAILGUN_WEBHOOK_SIGNING_KEY` is configured for the live Mailgun webhook endpoint; fake-signature request to `/api/v1/email/mailgun/webhook` returns 400. | `ATTESTOR_MAILGUN_WEBHOOK_PROOF`; OPS-SWEEP-08 maps the route and adds direct verifier unit tests. | Repository proof does not show real Mailgun webhook binding or signing-key rotation. |
| `LP-EMAIL-WEBHOOK-REPLAY-STORE` | Live shadow / limited enforcement | `repo-gated` | Multi-instance deployment proves a captured SendGrid or Mailgun event cannot be replayed to a different instance within the signature freshness window. | `ATTESTOR_EMAIL_WEBHOOK_REPLAY_STORE_PROOF`; OPS-SWEEP-08 makes email provider webhooks require shared control-plane storage by default, with only an explicit single-node evaluation override. | Static tests do not prove live shared-store behavior under multi-instance routing. |
| `LP-FEDERATED-CALLBACK-RATE-LIMIT` | Live shadow | `repo-gated` | Abuse probe against `/api/v1/auth/saml/acs` and `/api/v1/auth/oidc/callback` shows over-limit requests receive 429 before callback verification, while legitimate IdP retry behavior still passes. | `ATTESTOR_FEDERATED_CALLBACK_RATE_LIMIT_PROOF`; OPS-SWEEP-09 applies the auth abuse guard before SAML ACS and OIDC callback processing. | Static tests do not prove live source-IP behavior or provider-specific retry compatibility. |
| `LP-ACCOUNT-MUTATION-AUDIT-CHAIN` | Live shadow / limited enforcement | `repo-gated` | Live deployment proves account-session mutations write hash-linked audit records in shared storage with account actor attribution and redacted request hashes. | `ATTESTOR_ACCOUNT_MUTATION_AUDIT_CHAIN_PROOF`; OPS-SWEEP-09 extends the admin audit ledger with `account_session` actor records and account action IDs. | Repository tests do not prove cross-instance chain integrity or deployed log retention. |
| `LP-ACCOUNT-MUTATION-IDEMPOTENCY` | Live shadow / limited enforcement | `repo-gated` | Multi-instance deployment proves `Idempotency-Key`-bound account-admin API-key, user, invite, and password-reset issue retries return the stored response, reject mismatched non-secret payload conflicts, and do not duplicate mutation/audit side effects across API replicas. | `ATTESTOR_ACCOUNT_MUTATION_IDEMPOTENCY_PROOF`; OPS-81 remediation wires account-admin mutations through the encrypted idempotency service and route tests. | Repository tests do not prove live multi-replica routing or shared PostgreSQL replay behavior for account-admin mutations. |
| `LP-SHARED-AUTH-ABUSE-STORE` | Live shadow | `repo-gated` | Multi-instance deployment proves auth abuse buckets share Redis state and a single source IP cannot bypass limits by rotating API replicas. | `ATTESTOR_SHARED_AUTH_ABUSE_STORE_PROOF`; `src/service/account/auth-abuse-guard.ts` fails closed in production-like runtime without Redis. | In-memory fallback cannot prove HA abuse throttling. |
| `LP-PIPELINE-IDEMPOTENCY` | Live shadow / limited enforcement | `repo-gated` | Multi-instance deployment proves an `Idempotency-Key`-bound `/api/v1/pipeline/run` or `/api/v1/pipeline/run-async` retry returns the same run/job response and consumes tenant quota only once. | `ATTESTOR_PIPELINE_IDEMPOTENCY_PROOF`; OPS-SWEEP-10 adds the pipeline idempotency store/service, encrypted replay records, shared control-plane backing, and route tests. | Repository tests do not prove live multi-replica routing or shared PostgreSQL replay behavior. |
| `LP-SHADOW-MUTATION-AUDIT-CHAIN` | Live shadow / limited enforcement | `repo-gated` | Live deployment proves every shadow POST/PATCH writes a hash-linked audit record under `tenant_context` actor type; chain integrity probe matches admin/account semantics. | `ATTESTOR_SHADOW_MUTATION_AUDIT_CHAIN_PROOF`; OPS-SWEEP-11 adds shadow mutation audit hooks, runtime `appendAdminAuditRecordState` wiring, and direct HTTP route tests. | Repository tests do not prove cross-instance chain integrity or deployed log retention. |
| `LP-SHADOW-MUTATION-RATE-LIMIT` | Live shadow | `repo-gated` | Abuse probe against shadow POST/PATCH mutation routes shows same-tenant same-route over-limit requests return 429 with `Retry-After`, while distinct mutation route scopes remain independently available. | `ATTESTOR_SHADOW_MUTATION_RATE_LIMIT_PROOF`; `src/service/http/routes/shadow-routes.ts`; `tests/service-shadow-routes-http.test.ts`; OPS-96. | Repository tests do not prove deployed proxy/header behavior, legitimate operator retry compatibility, or multi-instance abuse behavior. |
| `LP-SHADOW-MUTATION-IDEMPOTENCY` | Live shadow / limited enforcement | `repo-gated` | Multi-instance deployment proves `Idempotency-Key`-bound shadow POST/PATCH mutation retries return stored responses, reject mismatched request conflicts, and do not duplicate persistence/audit side effects across API replicas. | `ATTESTOR_SHADOW_MUTATION_IDEMPOTENCY_PROOF`; OPS-95 remediation wires shadow mutations through the encrypted idempotency service and route tests. | Repository tests do not prove live multi-replica routing or shared PostgreSQL replay behavior for shadow mutations. |
| `LP-RELEASE-ROUTE-ROLE-ENFORCEMENT` | Live shadow / limited enforcement | `repo-gated` | Live deployment proves a holder of `admin-read`, `admin-account-admin`, `admin-key-admin`, or `admin-release-admin` cannot perform release-review approve/reject/override or release-policy emergency freeze/rollback by claiming a stronger actor role header. | `ATTESTOR_RELEASE_ROUTE_ROLE_ENFORCEMENT_PROOF`; OPS-SWEEP-12 adds release-route credential-bound role allowlists and direct HTTP tests for actor-role escalation denial. | Repository tests do not prove deployed role-key separation or legacy superuser-key rotation. |
| `LP-HEALTH-DIAGNOSTIC-SPLIT` | Live shadow | `repo-gated` | Live deployment proves public `/api/v1/health` and `/api/v1/ready` responses expose only minimal liveness/readiness fields through the active edge/proxy path, while rich runtime diagnostics remain unavailable to unauthenticated callers. | `ATTESTOR_HEALTH_DIAGNOSTIC_SPLIT_PROOF`; OPS-112 remediation; `tests/service-core-routes.test.ts` locks minimal public response shape. | Repository tests do not prove deployed edge/proxy behavior or any legacy cached diagnostic response. |
| `LP-VERIFY-RATE-LIMIT` | Live shadow | `repo-gated` | Abuse probe against `/api/v1/verify` shows over-limit requests receive 429 before JSON parsing or PKI verification work, with source-IP behavior matching the deployed proxy chain. | `ATTESTOR_VERIFY_RATE_LIMIT_PROOF`; OPS-113 remediation; `tests/pipeline-verification-routes.test.ts` locks same-source 429 behavior. | Static tests do not prove live source-IP extraction, proxy header behavior, or legitimate verifier client burst compatibility. |
| `LP-CORS-CSRF-DEPLOYMENT-CONTRACT` | Live shadow / limited enforcement | `repo-gated` | Staging browser deployment proves credentialed CORS allows only exact trusted origins and cookie-authenticated account mutations fail without `x-attestor-csrf`, with `Sec-Fetch-Site: cross-site`, and with an untrusted `Origin`. | `ATTESTOR_CORS_CSRF_DEPLOYMENT_PROOF`; `src/service/request-context.ts`; `tests/account-session-cors-csrf-boundary.test.ts`; `docs/08-deployment/deployment.md`. | Repository tests prove service-side request guards and exact-origin config parsing only; deployed browser, proxy, CDN, and CORS headers remain live environment facts. |
| `LP-KEDA-REDIS-TLS` | Live shadow | `repo-gated` | KEDA Redis scaler TLS posture matches the runtime Redis URL and is verified under the selected provider overlay. | `ATTESTOR_KEDA_REDIS_TLS_PROOF`; KEDA TLS example and provider README boundary. | Static manifests do not prove the live Redis endpoint requires or rejects TLS. |
| `LP-KEDA-PROMETHEUS-AUTH` | Live shadow | `repo-gated` | KEDA Prometheus scaler query access is authenticated or explicitly network-bounded. | `ATTESTOR_KEDA_PROMETHEUS_AUTH_PROOF`; KEDA Prometheus TriggerAuthentication example and provider README boundary. | Static manifests do not prove the live Prometheus auth or namespace policy. |

## Capture Rule

Each captured proof must include:

- environment name,
- timestamp,
- command or probe,
- redacted output artifact,
- responsible operator,
- no-raw-data confirmation,
- remaining limitation.

Captured proof should be linked from this file, not pasted with secrets or raw
customer data.

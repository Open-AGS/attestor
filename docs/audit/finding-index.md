# Finding Index

This is the current-state index for important Attestor audit findings. It is
not a complete backfill of every historical low-severity note. It is the
canonical place to reconcile findings that affect posture, live-shadow
readiness, public claims, or P0/P1 execution.

Historical remediation documents remain evidence leaves. A finding is current
only when this file points to current repository evidence or a live proof gap.

## State Labels

- `open`: current repository evidence still supports the issue.
- `closed`: current repository evidence shows the issue is fixed.
- `disputed/closed`: original risk is contradicted or bounded by current repo
  evidence.
- `accepted limitation`: documented limitation accepted for the current scope.
- `live-proof-only`: repo-side work is complete; live environment evidence is
  still required.
- `needs live test`: cannot be proven from repository evidence alone.
- `needs ops proof`: requires cloud/IAM/KMS/network/runtime evidence.
- `not proven`: insufficient evidence to claim.

## Current High-Signal Findings

| Finding | Current state | Severity | Protected principle | Current evidence | Required action |
|---|---|---:|---|---|---|
| `B-081` scope tiebreak locale comparison | `closed` | Low/Medium | proof integrity; deterministic policy resolution | `docs/audit/REM-2026-POL-SCOPING-001.md`; `src/release-policy-control-plane/scoping.ts`; PR `#502`. | No further repo action unless non-ASCII scope identifiers are intentionally introduced. |
| `B-059` trusted proxy wildcard | `disputed/closed` | Low | fail-closed boundary; operational boundedness | `src/service/trusted-proxy.ts` requires `ATTESTOR_TRUSTED_PROXY_PEER_WILDCARD_OVERRIDE=accept-the-risk`. | Keep deployment docs explicit that wildcard is an exceptional operator override. |
| `B-069` Vault Transit timeout | `closed` | Low | availability; operational boundedness | `src/service/secret-envelope.ts` uses bounded Vault Transit timeout with configurable default. | No further repo action. Live Vault latency behavior remains an ops test. |
| `B-033` optional tenant payload binding | `closed` | Medium | tenant isolation | Generic admission rejects mismatched body tenants and binds omitted body tenants to the route tenant; protected release token issuance and introspection entries carry the route tenant; shadow route/dashboard artifact paths fail closed on foreign tenant records without disclosing the foreign tenant id; downstream integration proof keeps publication/binding tenant mismatch not ready with `source-artifact-chain-mismatch`. Evidence: `src/service/http/routes/generic-admission-routes.ts`; `src/service/http/routes/shadow-routes.ts`; `tests/generic-admission-routes.test.ts`; `tests/shadow-route-tenant-boundary.test.ts`; `tests/shadow-dashboard-routes.test.ts`; `tests/shadow-downstream-integration-proof.test.ts`. Source anchors: NIST SP 800-162 ABAC subject/object attribute authorization; OWASP authorization deny-by-default, every-request validation, and authorization test guidance. | Repo-side closed. This does not prove live shared-store/RLS tenant isolation, customer PEP no-bypass, or production deployment tenant isolation. |
| `B-025` demo CLI scenario path traversal | `closed` | Low | data minimization; demo safety | `scripts/demo-path-boundary.ts` constrains `demo-golden-refund.ts --scenario` to `fixtures/` and `render-proof-showcase.ts --from` to approved proof/evidence roots by default; `tests/golden-refund-reviewer-sandbox.test.ts` locks out-of-basedir negative CLI paths. | Operator-local override still exists via `--allow-outside-demo-root` and must not be used for public artifacts without redaction review. |
| `B-028` secret redaction coverage | `closed` | Low/Medium | data minimization and redaction | `scripts/secret-safe-output.ts` now redacts AWS, GCP, GitHub, JWT, private-key block, Slack, Anthropic, OpenAI, runtime marker, and anchored Bearer forms; `tests/production-readiness-secret-safe-output.test.ts` locks provider coverage and false-positive guards; `scripts/check/check-public-artifacts-redaction.mjs` scans public docs/evidence paths. | Keep scanner green before public demo artifacts; live/operator artifacts outside scanned roots still need separate review. |
| Admin API key blast radius | `partial / live-proof-only` | P1 | customer authority; operational boundedness; auditability | OPS-SWEEP-06 adds role-scoped admin credential support (`ATTESTOR_ADMIN_*_API_KEY`), route-level role allowlists, admin actor-role audit fields, admin auth rate limiting, and HTTP tests; OPS-SWEEP-12 extends credential-bound role enforcement to release-review and release-policy-control routes. Legacy `ATTESTOR_ADMIN_API_KEY` remains a superuser compatibility fallback. | Prove live role-scoped operator key deployment, release-route role-key separation, and legacy superuser-key rotation before stronger live/Phase 2 claims. |
| OPS-57 admin route single global bearer | `closed / live-proof-only` | P1 | customer authority; operational boundedness; auditability | `src/service/request-context.ts` accepts role-scoped admin keys; `src/service/http/routes/admin-routes.ts` and `src/service/http/release-admin-authorization.ts` enforce route role allowlists; `tests/service-admin-routes-http.test.ts`, `tests/release-policy-control-plane-admin-routes.test.ts`, and `tests/release-review-admin-routes.test.ts` lock role-scoped denial paths. Locking test: `tests/service-admin-routes-http.test.ts`, `tests/release-policy-control-plane-admin-routes.test.ts`, `tests/release-review-admin-routes.test.ts`. | Capture live proof that operators use role-scoped keys, release-route role-key separation is deployed, and the legacy superuser key is rotated or tightly held. |
| OPS-58 admin route HTTP-layer test coverage | `closed` | P1 | auditability; customer authority | `tests/service-admin-routes-http.test.ts`; `package.json` `test:service-admin-routes-http`. Locking test: `tests/service-admin-routes-http.test.ts`. | Extend coverage only when new admin routes are added. |
| OPS-59 admin route auth rate limit | `closed / live-proof-only` | P1 | operational boundedness; availability | `src/service/http/routes/admin-routes.ts` applies admin auth rate limiting before `currentAdminAuthorized`; `tests/service-admin-routes-http.test.ts` proves over-limit requests do not reach auth comparison. Locking test: `tests/service-admin-routes-http.test.ts`. | Capture live abuse-traffic proof if making live-shadow or limited-enforcement claims. |
| OPS-60 admin read audit absence | `accepted limitation` | P2 | auditability; customer authority | OPS-SWEEP-06 confirms admin GET routes remain read-only and do not write mutation audit records. | Revisit after live role-scoped operator deployment if detailed read forensics are required. |
| OPS-61 admin POST JSON parsing | `closed` | P2 | fail-closed boundary; data minimization | `src/service/http/routes/admin-routes.ts` now uses a shared JSON helper requiring `Content-Type: application/json`, object bodies, and fail-closed 400/415 responses; HTTP test locks the boundary. | No further repo action unless a route intentionally needs a non-JSON body. |
| OPS-62 admin listing upper bounds | `closed` | P2 | operational boundedness; availability | Admin listing/read routes now apply default limits and maximum caps; HTTP test locks the `/admin/accounts` default cap. | Add cursor pagination before promising large operator exports. |
| OPS-63 admin response API key material | `accepted limitation` | P2 | data minimization and redaction | OPS-SWEEP-06 confirms account/key issuance, rotation, and recovery return one-time clear-text API key material; `hosted-api-authorization-matrix.ts` now names response-side key material and redaction requirements. | Keep runtime/proxy response logging redacted; do not remove issuance response material without a replacement UX. |
| OPS-64 admin cookie/session pass-through | `disputed/closed` | P2 | customer authority | OPS-SWEEP-06 verifies admin routes do not read cookie/session helpers and only use bearer admin auth; bootstrap middleware above admin routes does not hydrate account session authority. | No repo action unless a future admin route consumes account-session context. |
| OPS-65 live proof gate / register drift | `closed / live-proof-only` | P1 | runtime readiness; auditability; no overclaim | `scripts/check/check-ops-live-shadow-readiness.mjs` now gates every `required` or `repo-gated` live proof register entry with explicit stage-aware `ATTESTOR_*_PROOF` env flags; `tests/ops-live-shadow-readiness.test.ts` locks live-shadow and limited-enforcement stages. Locking test: `tests/ops-live-shadow-readiness.test.ts`. | Capture the live proof artifacts; keep new register entries aligned with the gate. |
| OPS-66 observability secret-template safety | `closed` | P1 | data minimization and redaction; fail-closed boundary | Grafana Cloud and Grafana Alloy kustomizations no longer include placeholder `secret-template.yaml` files as applyable resources; READMEs require External Secrets or environment-owned Secrets; `tests/kubernetes-observability-bundle.test.ts` locks this. Locking test: `tests/kubernetes-observability-bundle.test.ts`. | Keep placeholder templates as examples only; do not commit real managed-backend credentials. |
| OPS-67 profile renderer fail-open emit | `accepted limitation` | P2 | operational boundedness; no overclaim | HA and observability profile READMEs now document that renderers emit artifacts for operator review even when SLO checks fail. | Do not apply failed-SLO render output without explicit operator decision; optional future `--require-slo` flag can hard-fail on SLO misses. |
| OPS-68 observability profile-generated alerts not wired | `disputed/closed` | P2 | runtime readiness; auditability | Current `ops/observability/prometheus/alerts.yml` already includes `AttestorAvailabilityErrorBudget*` and `AttestorLatencyErrorBudget*`; `tests/observability-bundle.test.ts` and `tests/hosted-observability-privacy-incident-evidence.test.ts` lock burn-rate alert presence. | No repo action unless profile-generated alert names diverge from committed alert rules. |
| OPS-69 AWS HA overlay HTTPS / WAF parity gap | `closed / live-proof-only` | P1 | release provenance; operational boundedness; runtime readiness | `ops/kubernetes/ha/providers/aws/alb-ingress.https.example.yaml` adds HTTPS listener, SSL redirect, ACM certificate placeholder, SSL policy, and WAFv2 placeholder; AWS provider README names HTTPS/WAF proof gates; `tests/kubernetes-ha-bundle.test.ts` locks the example. Locking test: `tests/kubernetes-ha-bundle.test.ts`. | Capture live AWS HTTPS/WAF proof with environment-owned ACM/WAF ARNs before AWS live-shadow claims. |
| OPS-70 AKS / Azure parity absence | `accepted limitation` | P2 | runtime readiness; no overclaim | `ops/kubernetes/ha/providers/README.md` explicitly states Azure / AKS is out of scope until a scoped overlay, profile, tests, and live proof entries land. | Do not claim AKS parity. Add AKS overlay only when a real customer or operator target requires it. |
| OPS-71 observability profile zero-budget divisor | `closed` | P2 | runtime readiness; operational boundedness | `scripts/render-observability-profile.ts` rejects targets that do not leave a non-zero rounded error budget; `tests/observability-profile-render.test.ts` covers the `availabilityTarget: 1` boundary. | Keep SLO profile validation fail-closed before writing Prometheus rules. |
| OPS-72 KEDA scaler TLS / cross-namespace auth boundary | `closed / live-proof-only` | P2 | operational boundedness; data minimization | KEDA README documents Redis TLS and Prometheus auth proof gates; TLS-on Redis and Prometheus TriggerAuthentication examples are present; live-shadow gate requires `ATTESTOR_KEDA_REDIS_TLS_PROOF` and `ATTESTOR_KEDA_PROMETHEUS_AUTH_PROOF`. | Capture KEDA Redis TLS and Prometheus auth/network proof when KEDA is enabled in live shadow. |
| OPS-73 email webhook HA replay store opt-in | `closed / live-proof-only` | P1 | replay and idempotency safety; fail-closed boundary | `src/service/application/email-webhook-service.ts` now requires shared control-plane storage by default for SendGrid/Mailgun webhook ingest; local/file-backed replay state requires explicit `ATTESTOR_EMAIL_WEBHOOK_ALLOW_LOCAL_STORE=accept-the-risk`; `tests/service-email-webhook-service.test.ts` locks both fail-closed and override paths. Locking test: `tests/service-email-webhook-service.test.ts`. | Capture `ATTESTOR_EMAIL_WEBHOOK_REPLAY_STORE_PROOF` in a multi-instance live deployment before HA replay-safety claims. |
| OPS-74 webhook signature verifier unit tests | `closed` | P1 | proof integrity; auditability | `tests/service-email-webhook-signature-verifiers.test.ts` directly exercises Mailgun HMAC and SendGrid ECDSA verifier positive and negative cases, including expired timestamps, malformed timestamps, bad signature material, unset keys, and malformed SendGrid public key config. Locking test: `tests/service-email-webhook-signature-verifiers.test.ts`. | Extend direct verifier tests if another email provider is added. |
| OPS-75 webhook surface rate-limit absence | `closed / live-proof-only` | P2 | operational boundedness; availability | `src/service/webhook-rate-limit.ts` and webhook route registrations apply per-provider source rate limiting before raw-body read and signature/service work; `tests/service-webhook-route-rate-limit.test.ts` proves over-limit requests short-circuit Stripe, SendGrid, and Mailgun service calls. | Capture live edge/source-IP behavior and provider retry compatibility before stronger live claims. |
| OPS-76 webhook secret env not gated as live proof | `closed / live-proof-only` | P2 | runtime readiness; operational boundedness; no overclaim | `scripts/check/check-ops-live-shadow-readiness.mjs` now gates `ATTESTOR_STRIPE_WEBHOOK_PROOF`, `ATTESTOR_SENDGRID_EVENT_WEBHOOK_PROOF`, `ATTESTOR_MAILGUN_WEBHOOK_PROOF`, and `ATTESTOR_EMAIL_WEBHOOK_REPLAY_STORE_PROOF`; `docs/audit/live-proof-register.md` names the corresponding LP entries. | Capture provider dashboard/endpoint proof and fake-signature probes without exposing secrets. |
| OPS-77 Mailgun signature body coverage | `accepted limitation` | P2 | replay and idempotency safety; no overclaim | Mailgun Send webhooks sign timestamp plus token with the HTTP webhook signing key; `src/service/mailgun-email-webhook.ts` implements that source-backed scheme and binds replay through timestamp freshness plus a domain-separated token digest. | Keep the limitation visible; rely on OPS-73 shared replay/idempotency storage for residual replay risk. |
| OPS-78 webhook timestamp strict parsing | `closed` | P3 | data minimization; fail-closed boundary | `src/service/mailgun-email-webhook.ts` and `src/service/sendgrid-email-webhook.ts` require strict unsigned integer timestamp strings in signature verifiers; direct verifier tests reject trailing junk. | No further repo action unless provider timestamp formats change. |
| OPS-79 federated auth callback rate-limit gap | `closed / live-proof-only` | P1 | operational boundedness; availability | `src/service/http/routes/account-routes.ts` now applies the existing auth abuse guard before SAML ACS and OIDC callback verification; `tests/service-account-routes-authorization.test.ts` proves over-limit requests return 429 before the verifier is called again. Locking test: `tests/service-account-routes-authorization.test.ts`. | Capture `ATTESTOR_FEDERATED_CALLBACK_RATE_LIMIT_PROOF` with live source-IP behavior and IdP retry compatibility. |
| OPS-80 account-internal mutation audit gap | `closed / live-proof-only` | P1 | auditability; customer authority | `src/service/admin-audit-log.ts` accepts `account_session` actor records; `src/service/bootstrap/http-route-builders.ts` writes account-session mutations through `appendAdminAuditRecordState`; `src/service/http/routes/account-routes.ts` records redacted account API-key, user/invite, password, passkey, MFA, and billing handoff mutations; route test locks API-key issue audit emission. Locking test: `tests/service-account-routes-authorization.test.ts`. | Capture `ATTESTOR_ACCOUNT_MUTATION_AUDIT_CHAIN_PROOF` in shared storage before stronger live/Phase 2 claims. |
| OPS-81 account-internal mutation idempotency gap | `closed repo-side / live-proof-only` | P2 | replay and idempotency safety; operational boundedness | Account-admin API-key, user, invite, and password-reset issue mutation routes now consume optional `Idempotency-Key` values through the existing encrypted idempotency service, replay stored responses on same-key/same-request retries, reject same-key/different non-secret payload conflicts with 409, and avoid echoing the raw key. Raw password values are excluded from idempotency fingerprints. `src/service/http/routes/account-routes.ts`; `src/service/bootstrap/http-route-builders.ts`; `src/service/bootstrap/api-route-runtime.ts`; `tests/service-account-routes-authorization.test.ts`. Source anchors: IETF HTTPAPI `Idempotency-Key` draft request-fingerprint/replay/conflict model; Stripe idempotent request replay model. Locking test: `tests/service-account-routes-authorization.test.ts`. | Capture `ATTESTOR_ACCOUNT_MUTATION_IDEMPOTENCY_PROOF` across live replicas/shared control-plane storage before HA retry-safety claims. Self-service auth/MFA/password flows remain outside this account-admin mutation slice. |
| OPS-82 account password policy length-only | `closed` | P2 | customer authority; data minimization | Account password acceptance now uses `src/service/account-password-policy.ts` with the existing 12-character floor plus common-password and account/user-identifier derived password rejection; account auth, user-management, password-change, invite, and password-reset paths call the shared validator. Source anchors: NIST SP 800-63B/63-4 verifier guidance; OWASP authentication/password guidance by control vocabulary. Locking tests: `tests/service-account-auth-service.test.ts`, `tests/service-account-user-management-service.test.ts`. | This is a local repository blocklist and context check, not a live breached-password corpus proof or enterprise IdP password-policy claim. |
| OPS-83 account-route JSON body parsing | `closed` | P2 | fail-closed boundary; data minimization | Account JSON routes now use `readAccountJsonBody()` backed by `acceptsJsonRequestBody()`, reject non-JSON request bodies with 415, reject malformed/non-object JSON with 400, and no longer use `.json().catch(() => ({}))`. Source anchors: OWASP REST Security and Input Validation guidance. Locking test: `tests/service-account-routes-authorization.test.ts`. | Keep SAML ACS form parsing separate; future account JSON mutation routes must use the shared helper. |
| OPS-84 OIDC callback uses GET | `accepted limitation` | P2 | proof integrity; data minimization | OIDC Authorization Code Flow returns `code` through the browser redirect; Attestor uses state, nonce, PKCE, and no token exposure in the user agent. | Keep access/proxy logs redacted for `?code=` and `?state=`; no repo behavior change unless the OIDC flow changes. |
| OPS-85 auth attempt bucket naming hygiene | `closed` | P3 | operational boundedness | Account route auth-abuse bucket construction now uses a typed `AuthAttemptKind` vocabulary plus `AUTH_ATTEMPT_KIND` constants for action-token, current-password, federated-callback, and password-reset issue buckets. Locking tests: `tests/service-account-routes-authorization.test.ts`, `tests/account-auth-abuse-guard.test.ts`. | Keep the vocabulary explicit when new hosted-auth abuse buckets are added. |
| OPS-86 pipeline route idempotency-key gap | `closed / live-proof-only` | P1 | replay and idempotency safety; customer authority; operational boundedness | Pipeline `/run` and `/run-async` now consume `Idempotency-Key` before quota/rate/connector work, persist encrypted replay responses through file or shared control-plane storage, return the same response on matching replay, and return 409 on payload conflicts; `tests/service-pipeline-routes-idempotency.test.ts` locks sync replay, async replay, conflict, and missing-store fail-closed behavior. Locking test: `tests/service-pipeline-routes-idempotency.test.ts`. | Capture `ATTESTOR_PIPELINE_IDEMPOTENCY_PROOF` across live replicas and shared PostgreSQL before HA replay-safety claims. |
| OPS-87 pipeline `/run` handler decomposition | `closed` | P2 | auditability; fail-closed boundary | `/api/v1/pipeline/run` now lifts finance filing release-token/evidence-pack issuance, finance communication shadow evaluation, finance action shadow evaluation, and auto-filing generation into named route-stage helpers in `src/service/http/routes/pipeline-execution-routes.ts`; `tests/service-route-boundary.test.ts` locks the helper split and `tests/service-pipeline-routes-idempotency.test.ts` preserves replay/conflict behavior. Source anchors: NIST SP 800-218 secure-development vocabulary; OWASP API4 resource-consumption controls. Locking tests: `tests/service-route-boundary.test.ts`, `tests/service-pipeline-routes-idempotency.test.ts`. | This is a behavior-preserving route-stage decomposition, not a live Redis/Postgres transaction proof, external connector cost-limit proof, or claim that the pipeline surface is production-ready. Keep `ATTESTOR_PIPELINE_IDEMPOTENCY_PROOF` live-proof-only for HA retry/replay claims. |
| OPS-88 pipeline route JSON body parsing | `closed` | P2 | fail-closed boundary; data minimization | `src/service/http/route-response-helpers.ts` adds a shared JSON media-type pre-check, and `/api/v1/pipeline/run` plus `/api/v1/pipeline/run-async` reject non-JSON request bodies with 415 before parsing or side effects while malformed JSON remains 400. Locking test: `tests/service-pipeline-routes-idempotency.test.ts`. | Keep strict JSON pre-checks on future pipeline mutation routes. |
| OPS-89 async submission-consume atomicity | `closed` | P2 | replay and idempotency safety; customer authority | BullMQ `/run-async` submissions now derive a deterministic queue `jobId` from the tenant, route, and `Idempotency-Key` digest before enqueue, so a retry after queue-success / usage-consume failure reuses the same queue claim instead of creating a second random job. Source anchors: BullMQ custom job-id duplicate semantics; Stripe idempotent retry response model; OWASP API4 resource-consumption controls. Locking test: `tests/service-pipeline-routes-idempotency.test.ts`. | This is a repo-side compensating claim, not a live Redis/Postgres distributed transaction proof. Keep `ATTESTOR_PIPELINE_IDEMPOTENCY_PROOF` live-proof-only before HA replay/usage claims. |
| OPS-90 pipeline route error message redaction | `closed` | P2 | data minimization and redaction | Pipeline connector failures, filing-package failures, async worker failures, and generic route catch paths now return stable bounded messages instead of raw `error.message` values while preserving `proofMode: unavailable` for connector failures. Locking test: `tests/service-pipeline-routes-idempotency.test.ts`. | Keep connector/provider internals out of client problem details; use logs/proof artifacts only through redacted channels. |
| OPS-91 pipeline run history is decision log + signed report | `accepted limitation` | P2 | auditability; consequence-engine design | Pipeline runs do not write a flat tenant run-history row; the consequence-engine audit substrate remains decision log, signed report, and evidence chain. | Document this no-overclaim boundary in the pipeline/consequence runtime note before stronger audit-history claims. |
| OPS-92 pipeline runId nonce hygiene | `closed` | P3 | replay and idempotency safety; auditability | `src/service/http/route-response-helpers.ts#opaqueRouteRunId` now emits UUID-backed opaque run IDs; sync pipeline, async BullMQ input, in-process async job IDs, and in-process async run IDs no longer rely on millisecond timestamp strings. Locking test: `tests/service-pipeline-routes-idempotency.test.ts`. | Keep public pipeline identifiers opaque; do not reintroduce timestamp-only identifiers. |
| OPS-93 shadow mutation audit chain gap | `closed / live-proof-only` | P1 | auditability; customer authority | `src/service/http/routes/shadow-routes.ts` now records redacted shadow mutation audit inputs for successful POST/PATCH routes; `src/service/bootstrap/routes.ts#createShadowRouteDeps` writes `tenant_context` actor records through `appendAdminAuditRecordState`; `tests/service-shadow-routes-http.test.ts` locks audit emission for all shadow mutation route families. Locking test: `tests/service-shadow-routes-http.test.ts`. | Capture `ATTESTOR_SHADOW_MUTATION_AUDIT_CHAIN_PROOF` in shared storage before stronger live shadow audit-chain claims. |
| OPS-94 shadow route HTTP-layer test coverage gap | `closed` | P1 | auditability; no overclaim | `tests/service-shadow-routes-http.test.ts` exercises all 27 registered shadow routes, `cache-control: no-store`, successful mutation audit emission, missing-store fail-closed behavior, and JSON rejection paths. Locking test: `tests/service-shadow-routes-http.test.ts`. | Extend this direct route coverage when new shadow routes are added. |
| OPS-95 shadow mutation idempotency | `closed repo-side / live-proof-only` | P2 | replay and idempotency safety; auditability | Shadow POST/PATCH mutation routes now consume optional `Idempotency-Key` values through the encrypted pipeline idempotency service, replay stored same-key/same-request responses without duplicating persistence/audit side effects, reject same-key/different-request conflicts with 409, and do not echo raw keys. `hosted-api-authorization-matrix.ts` now declares `tenant_shadow_idempotency_service`; `src/service/bootstrap/routes.ts` passes the idempotency service into shadow route deps; `tests/service-shadow-routes-http.test.ts` locks replay, conflict, no-raw-key echo, and rate-limit replay behavior. Source anchors: IETF HTTPAPI `Idempotency-Key` draft request-fingerprint/replay/conflict model; Stripe idempotent request replay model. Locking test: `tests/service-shadow-routes-http.test.ts`. | Capture `ATTESTOR_SHADOW_MUTATION_IDEMPOTENCY_PROOF` across live replicas/shared control-plane storage before HA retry-safety claims. |
| OPS-96 shadow POST/PATCH rate-limit gap | `closed repo-side / live-proof-only` | P2 | operational boundedness; availability | Shadow POST/PATCH mutation routes now share a tenant+route scoped fixed-window guard with 429, `Retry-After`, and rate-limit headers, aligned with RFC 6585 429 semantics and OWASP API4 resource-consumption guidance; `tests/service-shadow-routes-http.test.ts` proves same-route retries are rejected while a separate shadow mutation route remains available. Locking test: `tests/service-shadow-routes-http.test.ts#testShadowMutationRateLimitIsTenantRouteScoped`. | Capture `ATTESTOR_SHADOW_MUTATION_RATE_LIMIT_PROOF` before claiming deployed abuse boundedness, proxy/header compatibility, or multi-instance behavior. |
| OPS-97 shadow GET listings lack pagination | `closed` | P2 | operational boundedness; availability | Shadow simulations, derived policy candidates, policy candidate records, and customer activation receipts now apply bounded default/max `limit` and offset `cursor` pagination with `pageInfo` while preserving total-count metadata, aligned with OWASP API4 resource-consumption guidance. Locking test: `tests/service-shadow-routes-http.test.ts#testShadowListRoutesApplyPaginationBounds`. | Keep shadow list routes bounded; replace offset cursors only if production store semantics require a stronger cursor contract. |
| OPS-98 shadow content-type mismatch returns 400 instead of 415 | `closed` | P3 | data minimization; fail-closed boundary | Shadow mutation body helpers now use the shared JSON media-type pre-check and return 415 for non-JSON request bodies while keeping malformed JSON at 400. Locking test: `tests/service-shadow-routes-http.test.ts`. | Keep new shadow mutation helpers aligned with the shared strict JSON pre-check. |
| OPS-99 shadow error message redaction | `closed` | P3 | data minimization and redaction | `src/service/http/routes/shadow-routes.ts` now routes caught constructor/store failures through bounded problem-detail helpers, aligned with OWASP REST error-handling guidance; status classifiers may inspect route-owned markers, but client details remain stable messages instead of raw `error.message`. Locking test: `tests/service-shadow-routes-http.test.ts#testShadowProblemDetailsAreBounded`. | Keep shadow problem details code-shaped and bounded; raw store/provider details belong only in redacted internal logs or proof channels. |
| OPS-100 release-route role-scoped admin enforcement gap | `closed / live-proof-only` | P1 | customer authority; operational boundedness; auditability | `src/service/http/release-admin-authorization.ts` enforces credential-bound release admin roles; `src/service/http/routes/release-review-routes.ts` and `src/service/http/routes/release-policy-control-routes.ts` apply read/mutation/break-glass allowlists; `tests/release-policy-control-plane-admin-routes.test.ts` and `tests/release-review-admin-routes.test.ts` prove read/release keys cannot escalate through `x-attestor-admin-actor-role`. Locking test: `tests/release-policy-control-plane-admin-routes.test.ts`, `tests/release-review-admin-routes.test.ts`. | Capture `ATTESTOR_RELEASE_ROUTE_ROLE_ENFORCEMENT_PROOF` in live deployment; keep legacy superuser-key rotation/holding visible. |
| OPS-101 release-policy-control typed-error gap | `closed` | P2 | fail-closed boundary; operational boundedness | `src/service/http/routes/release-policy-control-routes.ts` now maps route-owned validation errors, not-found, conflict, forbidden, and unknown/internal failures through bounded `PolicyControlPlaneError` codes and HTTP status mappings; unknown errors return a generic 500 message instead of echoing raw exception text. Locking test: `tests/release-policy-control-plane-admin-routes.test.ts#testTypedErrorsUseBoundedStatusMappings`. | Keep release-policy-control route errors code-shaped and do not reintroduce broad 400/raw `error.message` catch blocks. |
| OPS-102 release-policy-control listing pagination gap | `closed` | P2 | operational boundedness; availability | `/packs`, `/packs/:packId/bundles`, `/packs/:packId/versions`, `/activations`, and `/activation-approvals` now use bounded default/max `limit` and offset cursor pagination while preserving their list response keys. Locking test: `tests/release-policy-control-plane-admin-routes.test.ts#testListRoutesApplyPaginationBounds`. | Keep list routes bounded; add real cursor-store support later only if large operator exports need it. |
| OPS-103 release-review HTML security headers gap | `closed` | P2 | data minimization and redaction; fail-closed boundary | `secureHtmlResponseHeaders()` adds CSP, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, `X-Frame-Options: DENY`, and `no-store`; release-review inbox and detail HTML routes use it. Locking test: `tests/release-review-admin-routes.test.ts`. | Keep HTML helpers CSP-tuned if future release-review markup adds external assets or scripts. |
| OPS-104 break-glass body/header coercion inconsistency | `accepted limitation` | P3 | operational boundedness | Break-glass acknowledgement accepts strict body boolean `breakGlass === true` or header `x-attestor-break-glass: true`. | Keep both paths and document the contract if the release-policy matrix text is updated. |
| OPS-105 release-policy-control default actor role | `closed` | P3 | auditability | `src/service/http/release-admin-authorization.ts` now separates the admin authorization role header from the policy-domain actor label header: `x-attestor-admin-actor-role` remains credential-bound admin authorization input, while `x-attestor-policy-actor-role` is only copied into `ReleaseActorReference.role` for approval/audit semantics. `tests/release-policy-control-plane-admin-routes.test.ts` verifies policy actor labels cannot escalate an `admin-read` credential into mutation authority. Source anchors: NIST SP 800-162 ABAC subject attributes and OWASP access-control deny-by-default/server-side enforcement guidance. | Repo-side closed. Policy-domain actor labels remain audit/approval metadata only; they do not grant admin-route authority or prove live role-key deployment. |
| OPS-106 onboarding route shadow-event tenant defense-in-depth | `closed` | P2 | tenant isolation; data minimization | Action-surface and Policy Foundry hosted onboarding routes now assert every included shadow event is null-tenant public evidence or belongs to the authenticated tenant before packet/workflow construction. Locking test: `tests/action-surface-onboarding-packet-route.test.ts`. | Keep dependency-returned shadow events tenant-asserted even when upstream stores already filter by tenant. |
| OPS-107 policy-foundry hosted onboarding HTML security headers | `closed` | P2 | data minimization and redaction; fail-closed boundary | The hosted onboarding workflow HTML view now uses `secureHtmlResponseHeaders()` with CSP, `nosniff`, `no-referrer`, `DENY`, and `no-store`. Locking test: `tests/policy-foundry-hosted-onboarding-workflow-route.test.ts`. | Keep hosted review HTML review-only; this does not claim production readiness or enforcement activation. |
| OPS-108 policy-foundry wizard state idempotency | `closed` | P2 | replay and idempotency safety; auditability | `src/service/http/routes/policy-foundry-hosted-onboarding-routes.ts` now wires persisted wizard-state POSTs with `Idempotency-Key` through the existing pipeline idempotency service, returning stored responses on retry and 409 on same-key/different-body conflicts without echoing the raw key. `src/service/bootstrap/routes.ts` passes the service into the Policy Foundry route deps. Source anchors: IETF HTTPAPI `Idempotency-Key` draft request-fingerprint/replay/conflict model; Stripe idempotent request replay model. Locking test: `tests/policy-foundry-hosted-onboarding-workflow-route.test.ts`. | This is repo-side retry/replay safety for persisted wizard-state requests, not live multi-replica proof or production readiness. Shared-store behavior remains covered by live replay/idempotency proof gates before stronger HA claims. |
| OPS-109 onboarding/admission content-type mismatch status | `closed` | P3 | data minimization; fail-closed boundary | Generic admission, action-surface onboarding, and Policy Foundry hosted onboarding body readers now reject non-JSON request bodies with 415 before JSON parsing while malformed JSON remains 400. Locking test: `tests/generic-admission-routes.test.ts`, `tests/action-surface-onboarding-packet-route.test.ts`, `tests/policy-foundry-hosted-onboarding-workflow-route.test.ts`. | Keep 415/400 split on future onboarding/admission body readers. |
| OPS-110 admission problem-detail redaction | `closed` | P3 | data minimization and redaction | Generic admission downstream catch paths now return bounded route-owned problem details for agent-loop guard, protected-token issuer, shadow recording, and invalid-input failures instead of echoing arbitrary `error.message` values. Locking test: `tests/generic-admission-routes.test.ts`. | Tenant-scope mismatch and plan-mode denial remain explicit route-owned reasons; do not echo downstream provider messages in future admission catch paths. |
| OPS-111 action-surface bounds visibility | `accepted limitation` | P3 | operational boundedness | `MAX_HOSTED_MANIFESTS = 20` and `MAX_HOSTED_DECLARATIONS = 500` are enforced inside onboarding helpers but are not surfaced in matrix or response metadata. | Document the caps in the matrix and optionally echo configured caps in the response. |
| OPS-112 unauthenticated health/ready diagnostic disclosure | `closed / live-proof-only` | P1 | data minimization and redaction; no overclaim | `src/service/http/routes/core-routes.ts` now keeps unauthenticated `/api/v1/health` and `/api/v1/ready` responses minimal (`status`/`ready`/`version`/`engine`) and omits runtime profile, release runtime, shared store, storage path, connector, PKI, account-auth, and HA diagnostics; `tests/service-core-routes.test.ts` locks the absence of those diagnostic fields. Locking test: `tests/service-core-routes.test.ts`. | Capture live proof `LP-HEALTH-DIAGNOSTIC-SPLIT` that deployed public probes expose only the minimal response through the active edge/proxy path. |
| OPS-113 public verify route rate-limit gap | `closed / live-proof-only` | P1 | operational boundedness; availability | `src/service/public-route-rate-limit.ts` and `src/service/http/routes/pipeline-verification-routes.ts` apply a source-scoped `/api/v1/verify` rate limit before JSON parsing or PKI verification work; `tests/pipeline-verification-routes.test.ts` proves over-limit same-source requests return 429 with `Retry-After` and reset headers. Locking test: `tests/pipeline-verification-routes.test.ts`. | Capture live proof `LP-VERIFY-RATE-LIMIT` that deployed edge/source-IP behavior throttles before expensive verification work. |
| OPS-114 verify route structured logging gap | `closed` | P2 | auditability; operational boundedness | `/api/v1/verify` now emits `logger.warn('api.verify', ...)` rejection events with bounded reason codes and request-shape booleans only; raw certificate/key material is not logged. Locking test: `tests/pipeline-verification-routes.test.ts`. | Keep verify rejection logs structured and redacted; do not reintroduce route-local `console.log` for security-relevant denials. |
| OPS-115 public-site HTML security headers gap | `closed` | P2 | data minimization and redaction; fail-closed boundary | Public-site HTML routes now reuse `secureHtmlResponseHeaders()` for CSP, `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, and `cache-control: no-store`; HTML committed evidence is served through the same helper. Locking test: `tests/public-site-route-security.test.ts`. | Keep the shared helper on future public-site HTML responses; do not loosen CSP without route-specific review. |
| OPS-116 public-site cache-control gap | `closed` | P2 | operational boundedness; data minimization | Public-site dynamic HTML, committed evidence assets, and the legacy `/app` redirect now emit `cache-control: no-store`; non-HTML evidence assets also emit `X-Content-Type-Options: nosniff` and `Referrer-Policy: no-referrer`. Locking test: `tests/public-site-route-security.test.ts`. | Use stronger immutable caching only for future content-addressed public assets with stable digest URLs. |
| OPS-117 filing-export error message redaction | `closed` | P3 | data minimization and redaction | `/api/v1/filing/export` now returns route-owned generic problem details for unexpected filing failures and route-owned `invalid_token` descriptions for untyped release-verification failures; raw exception text stays out of the client body and `WWW-Authenticate` challenge. Locking test: `tests/pipeline-filing-default-secure.test.ts`. | Keep typed `ReleaseVerificationError` responses controlled by the release-verification layer; do not echo downstream/provider exception text in generic catch paths. |
| OPS-118 verify error message redaction | `closed` | P3 | data minimization and redaction | `/api/v1/verify` now returns `internal_error` with route-owned `Verification failed.` for unexpected verifier failures and `invalid_request` for malformed JSON instead of echoing raw `err.message` values. Locking test: `tests/pipeline-verification-routes.test.ts`. | Keep verifier internals in redacted structured logs only; public problem details remain bounded. |
| OPS-119 startup diagnostic leakage | `closed` | P3 | data minimization; no overclaim | `/api/v1/startup` now exposes only bounded startup status, version, uptime, and product identity; unauthenticated callers no longer receive `instanceId` or runtime-profile production flags. Locking test: `tests/service-core-routes.test.ts`, `tests/f8-operational-resilience-validation.test.ts`. | Rich runtime diagnostics remain outside the unauthenticated startup probe; live edge/proxy proof remains covered by `LP-HEALTH-DIAGNOSTIC-SPLIT`. |
| OPS-120 connector configured-state disclosure | `closed` | P3 | data minimization; no overclaim | `/api/v1/connectors` now returns only public catalog metadata (`id`, `displayName`), adds `cache-control: no-store`, and does not call `loadConfig()` or `isAvailable()` for unauthenticated catalog responses. Locking test: `tests/service-core-routes.test.ts`. | Keep configured/available connector state in authenticated/operator diagnostics only; do not infer connector runtime readiness from the public catalog. |
| OPS-121 branch protection signed-commits gap | `closed repo-side / live-proof-only` | P1 | release provenance | `.github/workflows/branch-governance.yml` now verifies `required_signatures.enabled == true` through the GitHub branch protection API on scheduled/manual/master-push governance runs using a dedicated `ATTESTOR_BRANCH_GOVERNANCE_TOKEN` admin-read secret; `tests/branch-governance-policy.test.ts` locks the probe shape and token boundary. Locking test: `tests/branch-governance-policy.test.ts`. | Enable the GitHub setting on `master`, configure the dedicated governance token secret, and capture `LP-BRANCH-PROTECTION-SIGNATURES`, including unsigned-commit negative merge proof, before stronger release-provenance claims. |
| OPS-122 branch protection required-reviews gap | `closed repo-side / live-proof-only` | P1 | release provenance; auditability | `.github/workflows/branch-governance.yml` now verifies `required_pull_request_reviews`, `required_approving_review_count >= 1`, `require_code_owner_reviews == true`, `dismiss_stale_reviews == true`, and `require_last_push_approval == true` using a dedicated `ATTESTOR_BRANCH_GOVERNANCE_TOKEN` admin-read secret; `tests/branch-governance-policy.test.ts` locks the probe shape and token boundary. Locking test: `tests/branch-governance-policy.test.ts`. | Enable the GitHub settings on `master`, configure the dedicated governance token secret, and capture `LP-BRANCH-PROTECTION-REVIEWS`, including same-author/self-merge negative proof, before stronger release-provenance claims. |
| OPS-123 branch-protection live-proof flag absence | `closed` | P2 | runtime readiness; no overclaim | `scripts/check/check-ops-live-shadow-readiness.mjs` now requires `ATTESTOR_BRANCH_PROTECTION_SIGNATURES_PROOF` and `ATTESTOR_BRANCH_PROTECTION_REVIEWS_PROOF` at live-shadow stage; `live-proof-register.md` carries `LP-BRANCH-PROTECTION-SIGNATURES` and `LP-BRANCH-PROTECTION-REVIEWS`; `tests/ops-live-shadow-readiness.test.ts` locks both flags. | Capture the two live proofs before treating branch protection as live-proven. |
| OPS-124 branch-governance drift detection lag | `closed` | P2 | operational boundedness; auditability | `branch-governance.yml` now runs on PRs and pushes touching `.github/**` or `SECURITY.md`, while keeping scheduled and manual runs. Pull-request runs avoid live GitHub setting probes; scheduled/manual/master-push runs enforce the live settings. | Keep branch-governance CI green after GitHub branch protection settings are enabled. |
| OPS-125 CODEOWNERS single-owner gap | `blocked / operator-decision` | P2 | release provenance; auditability | `.github/CODEOWNERS` routes all trust-sensitive surfaces to `@AI-gateway-systems`; branch governance asserts code-owner review enforcement, but repo/GitHub evidence does not identify visible write-enabled per-surface teams or a team membership policy. `CONTRIBUTING.md` and `.github/CODEOWNERS` now forbid placeholder team slugs until those operator-owned teams exist. Source anchors: GitHub CODEOWNERS docs require team owners to be visible and have explicit write access; GitHub protected-branch docs describe required code-owner reviews. Locking test: `tests/branch-governance-policy.test.ts`. | Operator must create visible write-enabled GitHub teams and document membership policy before a real per-surface CODEOWNERS split. Do not mark this repo-side closed by inventing team names. |
| OPS-126 Dependabot PR contract bypass scope | `accepted limitation` | P2 | release provenance; auditability | `validate-pr-contract.mjs` explicitly bypasses the PR contract for `dependabot[bot]`, including dependency PRs that may touch runtime/security/build/action surfaces. | Keep the bypass for routine updates; require explicit human merge approval for Dependabot PRs touching critical surfaces. |
| OPS-127 release-provenance evaluation-tag scope | `accepted limitation` | P3 | release provenance; no overclaim | `release-provenance.yml` triggers on `v*-evaluation` tags and manual dispatch only. | Keep for Phase 1 evaluation posture; revisit at Phase 2 before production-tag provenance claims. |
| OPS-128 workflow permission-scope CONTRIBUTING note | `closed` | P3 | release provenance; auditability | `CONTRIBUTING.md` now documents workflow permission discipline: default `contents: read`, no broad `write-all` / `contents: write`, and PR justification plus CODEOWNER review for elevated scopes; `.github/CODEOWNERS` owns the guidance; `tests/branch-governance-policy.test.ts` locks the guidance and owner mapping. Source anchors: GitHub Actions `permissions` syntax and GitHub `GITHUB_TOKEN` least-required-access guidance. Locking test: `tests/branch-governance-policy.test.ts`. | Keep new write-scoped workflow permissions tied to a workflow-local reason, protected principle, and regression check; OPS-125 per-surface CODEOWNER split remains blocked/operator-decision until operator-owned team evidence exists. |
| OPS-129 redaction provider-pattern gap | `closed` | P1 | data minimization and redaction; demo safety; no overclaim | `scripts/secret-safe-output.ts` extends `SENSITIVE_OUTPUT_PATTERNS` with conservative AWS `AKIA`/`ASIA`, GCP `AIza`/`ya29.`, GitHub PAT, JWT, private-key block, Slack, Anthropic, and OpenAI-specific patterns. `tests/production-readiness-secret-safe-output.test.ts` proves one positive sample per provider class. Supersedes the highest-risk part of `B-028`. Locking test: `tests/production-readiness-secret-safe-output.test.ts`. | Keep provider patterns conservative and update the matrix before adding new public provider output. |
| OPS-130 redaction policy / implementation drift | `closed` | P2 | data minimization and redaction; auditability | `tests/production-readiness-secret-safe-output.test.ts` imports `CONSEQUENCE_DATA_MINIMIZATION_RUNTIME_SECRET_MARKERS` and proves representative runtime marker samples are redacted by `scripts/secret-safe-output.ts`. | A future shared typed registry can still replace the test-based coverage assertion; not required for current public demo closure. |
| OPS-131 redaction provider test matrix gap | `closed` | P2 | auditability; no overclaim | `tests/production-readiness-secret-safe-output.test.ts` now covers AWS, GCP, GitHub, JWT, private-key block, Slack, Anthropic, OpenAI, runtime marker samples, a mixed provider set, and false-positive guards. | Extend this matrix before adding new provider token families to public/operator output. |
| OPS-132 public artifact redaction scan absence | `closed` | P2 | data minimization and redaction; demo safety | `scripts/check/check-public-artifacts-redaction.mjs`, `check:public-artifacts-redaction`, `test:public-artifacts-redaction`, and `.github/workflows/security-scan.yml` now scan `README.md`, `SECURITY.md`, `.well-known/security.txt`, `docs/00-evaluation/**`, and `docs/evidence/**` through the shared redactor. | Scanner covers committed public artifact roots, not arbitrary local/live artifacts. |
| OPS-133 demo CLI scenario path basedir constraint | `closed` | P3 | demo safety; no overclaim | `scripts/demo-path-boundary.ts` enforces default basedir constraints for `demo-golden-refund.ts --scenario` and `render-proof-showcase.ts --from`; `tests/golden-refund-reviewer-sandbox.test.ts` locks rejected out-of-root CLI paths. | `--allow-outside-demo-root` remains an operator-local override with a no-overclaim warning. |
| OPS-134 Bearer regex anchoring hygiene | `closed` | P3 | data minimization | `secret-safe-output.ts` now redacts short Bearer values only when header-anchored to `Authorization` / `Proxy-Authorization`, while generic Bearer redaction requires a longer token-like value with token punctuation; false-positive guard covers benign `Bearer of accountability`. | Keep future Bearer expansion anchored to headers or token-like shape. |
| OPS-135 policy marker case-handling | `accepted limitation` | P3 | data minimization | Runtime policy markers remain lowercase strings; OPS-130 now adds representative runtime marker coverage in `tests/production-readiness-secret-safe-output.test.ts`, but no shared typed regex registry was introduced. | Keep accepted until a future registry unification removes substring-marker drift entirely. |
| OPS-136 finding-index test-coverage guard absence | `closed` | P1 | auditability; no overclaim | `scripts/check/check-finding-test-coverage.mjs` now cross-checks closed P0/P1 `finding-index.md` rows for a structured `Locking test:` marker and existing `tests/*.test.ts` paths; `.github/workflows/evaluation-smoke.yml` runs `test:audit-finding-test-coverage`; `tests/finding-test-coverage.test.ts` covers positive, negative, missing-file, disputed-closure, and live-contract cases. Locking test: `tests/finding-test-coverage.test.ts`. | Keep the guard wired in reviewer-facing checks before closing future P0/P1 findings. |
| OPS-137 OPS-25 alertmanager renderer-output test gap | `stale/closed` | P2 | runtime readiness; operational boundedness | Current `origin/master` contradicts the old gap: `tests/alertmanager-config-render.test.ts` invokes `scripts/render-alertmanager-config.mjs` with synthetic routing env, asserts critical/warning/default/security/billing/watchdog receivers and delivery config render, and proves invalid production config fails fast. | No further repo action for this scoped renderer-output gap; live alert delivery remains `LP-OBSERVABILITY-ALERT-DELIVERY`. |
| OPS-138 needs-live-test rows without future test contract | `closed` | P2 | auditability; no overclaim | `Customer PEP no-bypass`, `External KMS runtime signing`, and `Shared replay / introspection store` now name future `tests/live-*.test.ts` contract paths, and `scripts/check/check-finding-test-coverage.mjs` fails P0/P1 `needs live test` / `needs ops proof` rows without those markers. | Future live tests still need implementation and real environment evidence before any live-proof claim closes. |
| OPS-139 finding-index `Locking test:` marker absence | `closed` | P2 | auditability | Closed P0/P1 rows now use a structured `Locking test:` convention, and `scripts/check/check-finding-test-coverage.mjs` consumes that marker instead of relying on free-text evidence search. | Extend the marker whenever future P0/P1 rows move to a repo-closed state. |
| OPS-140 OPS-26 / OPS-30 implicit locking-test references | `closed` | P3 | auditability | OPS-26 and OPS-30 now carry explicit `Locking test:` markers naming `tests/observability-bundle.test.ts`, `tests/observability-credentials-render.test.ts`, and `tests/ops-sweep-03-observability-remediation.test.ts`. | Keep the markers aligned if observability tests move or split. |
| OPS-141 keyless CA lazy fallback in production-like runtime | `closed` | P1 | proof integrity; key authority | `src/signing/keyless-signer.ts#getOrCreateCa` now throws before auto-generating a process-local CA when `NODE_ENV=production`, `ATTESTOR_HA_MODE`, `ATTESTOR_PUBLIC_HOSTNAME`, or `ATTESTOR_PUBLIC_BASE_URL` marks the runtime production-like; `configureReleaseRuntimeKeylessCa` remains the release-runtime path. Locking test: `tests/f5-keyless-ca-injection-boundary-validation.test.ts`. | Repo-side closed for fail-closed production-like behavior. Live/operator proof remains tracked by `LP-KEYLESS-CA-PRODUCTION-GUARD`; this does not close external KMS runtime signing. |
| OPS-142 GCP KMS adapter contract-ready but not runtime-wired | `accepted limitation` | P2 | proof integrity; key authority | OPS-SWEEP-18 confirms `src/service/bootstrap/gcp-kms-release-signer.ts` is contract-ready and uses explicit no-overclaim flags, but `activatesRuntimeSigning` is still literally `false` and no end-to-end KMS-signed artifact reaches downstream verification. Supersedes the runtime-wiring half of `External KMS runtime signing`. | Keep for Phase 1; close in Phase 2 with operator opt-in, live GCP KMS roundtrip, downstream verifier acceptance, and `LP-KMS-RUNTIME-SIGNING` capture. |
| OPS-143 signing evidence wording ambiguity | `disputed/closed` | P2 | no overclaim; auditability | OPS-SWEEP-19 closes the ambiguity: release-kernel evidence packs are DSSE + in-toto-shaped (`release-evidence-pack.ts` declares `application/vnd.in-toto+json`, in-toto Statement v1, DSSE envelope shape, and DSSE PAE), while the `src/signing/*` certificate/trust-chain primitive remains Attestor-specific canonical JSON and is not itself a DSSE/in-toto signer. | Keep proof/signing summaries scoped to the two layers; do not reopen unless docs again blur evidence-pack wire format and primitive signing material. |
| OPS-144 legacy certificate sunset not CI-asserted | `accepted limitation` | P2 | release provenance; no overclaim | OPS-SWEEP-18 confirms `LEGACY_UNBOUNDED_CERTIFICATE_SUNSET_AT = '2026-12-31T23:59:59.999Z'` is declared and warnings are tested, but no test enforces a future warning/fail policy as the sunset approaches. | Add a sunset-window CI test before Phase 2 or before the sunset approaches. |
| OPS-145 signing test layout inconsistency | `closed` | P3 | auditability; operational boundedness | The primary signing primitive test now lives at `tests/signing-primitives.test.ts`, and `package.json` keeps `test:signing` pointed at that suite. This preserves signing primitive coverage while aligning the critical test with the repository-wide `tests/*.test.ts` layout. Locking test: `npm run test:signing`. | Repo-side closed. This is test-layout hygiene only; it does not change signing runtime behavior or close external KMS runtime signing proof. |
| OPS-146 reset-CA-for-testing not production-blocked | `closed` | P3 | proof integrity; operational boundedness | `resetKeylessCaForTesting` now throws in the same production-like runtime conditions before accepting a test-only reason. Locking test: `tests/f5-keyless-ca-injection-boundary-validation.test.ts`. | Repo-side closed. The helper remains internal test support and is not a public package export. |
| OPS-147 OPS-143 evidence-pack and policy-bundle clarification | `disputed/closed` | P1 | proof integrity; no overclaim | OPS-SWEEP-19 confirms `src/release-kernel/release-evidence-pack.ts` implements DSSE + in-toto-shaped evidence packs (`RELEASE_EVIDENCE_PACK_DSSE_PAYLOAD_TYPE = 'application/vnd.in-toto+json'`, `RELEASE_EVIDENCE_PACK_STATEMENT_TYPE = 'https://in-toto.io/Statement/v1'`, DSSE envelope, and DSSE PAE). OPS-SWEEP-21 extends the same clarification to `src/release-policy-control-plane/bundle-format.ts` and `bundle-signing.ts`, which use the same DSSE payload type and in-toto Statement v1 shape for policy bundles. | No code action. Keep the distinction: release evidence pack and policy bundle wire formats are DSSE/in-toto-shaped; primitive signing/certificate/trust-chain material is Attestor-specific canonical JSON. |
| OPS-148 canonicalization parity test absence | `closed` | P2 | proof integrity; auditability | `tests/release-kernel-canonicalization-parity.test.ts` proves `src/signing/sign.ts#canonicalize` (`attestor.signing-canonical-json.v1`) and `src/release-kernel/release-canonicalization.ts#canonicalizeReleaseJson` (`attestor.release-canonicalization.v1`) produce byte-identical canonical JSON across shared fixtures while keeping the two spec ids explicit and rejecting ambiguous/lossy JSON values. Locking test: `tests/release-kernel-canonicalization-parity.test.ts`. | Repo-side closed. Keep the no-interop boundary: these remain Attestor-specific canonical JSON functions, not a general RFC 8785/JCS compatibility claim. |
| OPS-149 release decision log HA-safe writer documentation | `closed repo-side / live-proof-only` | P2 | replay and idempotency safety; auditability | The stale OPS-SWEEP-19 gap is now repo-side closed by the production-shared authority-plane track: `src/service/release-decision-log-store.ts` implements a PostgreSQL-backed shared release decision log with transaction-scoped advisory locking, contiguous sequence/hash-chain verification, tamper fail-closed reads, and `ATTESTOR_RELEASE_AUTHORITY_PG_URL` substrate wiring; `src/service/bootstrap/release-runtime.ts` cuts the production-shared request path over only after all eight shared authority stores probe successfully and marks `bootstrapWired=true`; `tests/release-decision-log-store.test.ts`, `tests/production-shared-request-path-cutover.test.ts`, and `tests/production-shared-multi-instance-recovery.test.ts` lock embedded-PostgreSQL shared-store behavior. Source anchors: PostgreSQL advisory locks/transaction isolation, node-postgres same-client transactions, and Kubernetes readiness semantics. Live proof gate: `LP-RELEASE-DECISION-LOG-SHARED-STORE` / `ATTESTOR_RELEASE_DECISION_LOG_SHARED_STORE_PROOF`. | Capture live customer-operated PostgreSQL, credentials, network, backup/restore, and multi-replica rehearsal evidence before claiming external production or limited-enforcement release decision log HA. |
| OPS-150 release decision log random entryId | `accepted limitation` | P2 | auditability; replay and idempotency safety | OPS-SWEEP-19 confirms `createReleaseDecisionLogEntry` uses `randomUUID()` for `entryId`; this is HA-safe but intentionally not replay-deterministic. | Keep. Tests should assert digest, sequence, and chain integrity rather than exact entry IDs. |
| OPS-151 first hard gateway wedge / dynamic policy parity test | `closed` | P2 | release provenance; no overclaim | `tests/release-kernel-first-hard-gateway-policy-parity.test.ts` asserts the frozen first hard gateway wedge, `createFirstHardGatewayReleasePolicy()`, R4 risk-control profile, and default `createReleaseDecisionEngine()` policy plan remain aligned for target kinds, consequence type, risk class, enforcement mode, review, token, signed-envelope, evidence, and downstream-receipt invariants. Locking test: `tests/release-kernel-first-hard-gateway-policy-parity.test.ts`. | Repo-side closed. Stronger first-gateway product claims still require downstream customer PEP/no-bypass proof. |
| OPS-152 deterministic check resource budget | `closed` | P3 | operational boundedness | `src/release-kernel/release-deterministic-checks.ts` exports `RELEASE_DETERMINISTIC_CHECK_OBSERVATION_BUDGET`, rejects over-budget observation arrays with `deterministic-check-resource-budget`, and returns a fail-closed deterministic report before category iteration. `tests/release-kernel-release-deterministic-checks.test.ts` covers adversarial over-budget observation arrays. Source anchors: OWASP API4:2023 Unrestricted Resource Consumption, CWE-400, and the Node.js event-loop guidance on bounding input size for callbacks. | Repo-side closed. This does not prove every future external route/API parser enforces its own payload-size boundary; keep route/API budgets aligned if new externally reachable deterministic-check inputs are added. |
| OPS-153 release token introspection production-bootstrap | `accepted limitation` | P3 | replay and idempotency safety; runtime readiness | OPS-SWEEP-19 confirms `createInMemoryReleaseTokenIntrospectionStore` is available and non-persistent; file-backed and shared-control-plane paths must be selected explicitly before HA/runtime claims. | Keep as bootstrap discipline and pair with existing shared replay/introspection live-proof work. |
| OPS-154 enforcement-plane line-by-line audit depth gap | `not proven` | P2 | auditability; no overclaim | OPS-SWEEP-20 audits `src/release-enforcement-plane/**` at spec, entry-point, export, and test-inventory depth across 22 files / 16,972 source lines. It does not line-by-line behaviorally audit every branch in the surface. | Keep as a non-blocking sweep-budget disclosure; spawn focused sub-sweeps for DPoP, offline verifier, Envoy ext_authz, token exchange, or degraded mode only when module-specific risk warrants it. |
| OPS-155 DPoP accepted-algorithm default policy is per-call | `closed` | P2 | proof integrity; fail-closed boundary | `src/release-enforcement-plane/dpop.ts` exports frozen `DEFAULT_ACCEPTED_DPOP_ALGORITHMS = [ES256, EdDSA]`, and `verifyDpopProof` now uses that module-level local-policy constant when callers do not pass an explicit allowlist. `tests/release-enforcement-plane-dpop-default-policy.test.ts` proves the default is frozen, accepts ES256/EdDSA, rejects RS256 by default, and accepts RS256 only when a caller explicitly expands the allowlist. Locking test: `tests/release-enforcement-plane-dpop-default-policy.test.ts`. | Repo-side closed. Future algorithm widening remains an explicit provider/operator policy decision with regression coverage. |
| OPS-156 HTTP message signatures Ed25519-only narrowing | `accepted limitation` | P2 | proof integrity; no overclaim | OPS-SWEEP-20 confirms `http-message-signatures.ts` pins `HTTP_MESSAGE_SIGNATURE_ALGORITHM = 'ed25519'` while RFC 9421 allows other algorithms such as RSA-PSS, ECDSA, and HMAC-SHA256. | Keep the narrower algorithm surface; document that future provider adapters require explicit allowlist expansion and regression tests. |
| OPS-157 token exchange RFC 8693 deep audit deferred | `closed` | P3 | release provenance; auditability | Dedicated token-exchange sub-sweep evidence now covers the RFC 8693-shaped request/result vocabulary for `grant_type`, `audience`, `resource`, `scope`, `requested_token_type`, `subject_token_type`, and actor-token handling. `src/release-enforcement-plane/token-exchange.ts` keeps Attestor's issued token type explicit, denies unsupported requested/subject token types, and fail-closes raw `actorToken` / orphan `actorTokenType` inputs because this repository surface supports structured actor references and nested `act` claims, not raw external actor-token validation. `tests/release-enforcement-plane-token-exchange.test.ts` locks audience/resource/scope narrowing, source audience, parent token linkage, actor-chain nesting, unsupported requested/subject token types, and raw actor-token no-support behavior. Source anchor: RFC 8693 Sections 2.1, 2.1.1, 3, and 4.1. Locking test: `tests/release-enforcement-plane-token-exchange.test.ts`. | Repo-side closed for the Attestor token-exchange contract. This is not an OAuth certification, does not add raw external `actor_token` support, and does not prove customer PEP/no-bypass or live replay/introspection stores. |
| OPS-158 Envoy ext_authz default risk class must not become route policy | `closed` | P3 | release provenance; operational boundedness | `src/release-enforcement-plane/envoy-ext-authz.ts` now accepts a validated `attestor.risk_class` Envoy route context extension, keeps explicit server `options.riskClass` precedence, and fails closed on invalid route risk values. `docs/08-deployment/release-enforcement-plane-envoy.md` documents that `ENVOY_EXT_AUTHZ_DEFAULT_RISK_CLASS` is a fallback only and production Envoy/Istio routes must bind explicit route risk. Locking test: `tests/release-enforcement-plane-envoy-ext-authz.test.ts`. Source anchors: Envoy `ExtAuthzPerRoute.check_settings.context_extensions`; Istio `CUSTOM` external authorization delegation. | Repo-side closed. This does not prove a live Envoy/Istio rollout, route-map deployment, customer PEP no-bypass, or production readiness. |
| OPS-159 degraded-mode maxUses deep audit deferred | `accepted limitation` | P3 | replay and idempotency safety; auditability | OPS-SWEEP-20 confirms `DEFAULT_DEGRADED_MODE_MAX_USES = 1`, but does not line-by-line audit custom `maxUses > 1` grant enforcement paths inside `degraded-mode.ts`. | Keep default max-use as 1; audit custom max-use increment/check behavior before operators allow `maxUses > 1`. |
| OPS-160 policy-bundle DSSE/in-toto clarification extension | `closed` | P2 | proof integrity; no overclaim | `src/release-policy-control-plane/bundle-format.ts` declares `POLICY_BUNDLE_PAYLOAD_TYPE = 'application/vnd.in-toto+json'` and `POLICY_BUNDLE_STATEMENT_TYPE = 'https://in-toto.io/Statement/v1'`; `bundle-signing.ts` defines a DSSE envelope and production signer non-claims. Baseline/control-map now scope DSSE/in-toto interop to release-kernel evidence packs and release-policy-control-plane policy bundles, while keeping `src/signing/*` primitive signing Attestor-specific canonical JSON. Locking test: `tests/release-policy-control-plane-proof-discipline.test.ts`. | Repo-side closed. Do not extend the DSSE/in-toto claim to primitive signing/certificate material. |
| OPS-161 policy mutation audit log shared-store proof gap | `closed / live-proof-only` | P2 | replay and idempotency safety; auditability | Policy mutation audit entries remain hash-chained in repo, but the current durable writer is file-backed and guarded by `withFileLock`; the live-proof register and ops gate now require `ATTESTOR_POLICY_MUTATION_AUDIT_CHAIN_PROOF` / `LP-POLICY-MUTATION-AUDIT-CHAIN-SHARED-STORE` before multi-instance policy-mutation audit claims. Locking test: `tests/release-policy-control-plane-proof-discipline.test.ts`. | Capture the limited-enforcement live proof with shared storage; file-backed writer remains evaluation/single-node evidence only. |
| OPS-162 activation approval store production-bootstrap discipline | `accepted limitation / live-proof-only` | P2 | replay and idempotency safety | Activation approval stores have in-memory and file-backed variants; neither proves multi-instance durability. The live-proof register and ops gate now require `ATTESTOR_POLICY_ACTIVATION_APPROVAL_STORE_PROOF` / `LP-POLICY-ACTIVATION-APPROVAL-SHARED-STORE` before multi-instance approval durability claims. Locking test: `tests/release-policy-control-plane-proof-discipline.test.ts`. | Accepted limitation for the current repo surface. Capture shared durable approval-store proof before stronger limited-enforcement claims. |
| OPS-163 approval state-machine and scope-precedence body audit deferred | `closed` | P2 | release provenance; auditability | Dedicated approval/scoping sub-sweep evidence now covers the activation approval state machine and scope precedence traversal at focused behavioral depth. `src/release-policy-control-plane/activation-approvals.ts` now rejects approval decisions recorded after the approval request expires and uses binary `compareCanonicalKeys` rather than `localeCompare` for same-timestamp request ordering. `tests/release-policy-control-plane-activation-approvals.test.ts` locks R4 dual approval, requester separation, late-decision denial, distinct reviewers, expired/mismatched/bundle-digest gates, risk-class TTLs, locale-independent digests, locale-independent list/snapshot ordering, and file-backed persistence. `tests/release-policy-control-plane-scoping.test.ts` locks environment boundaries, wildcard matching, hierarchy validation, identity/wedge/risk/cohort/plan precedence, no-match handling, most-specific winner selection, same-precedence ambiguity, locale-independent malformed-label ambiguity, and descriptor/manifest alignment. Source anchors: NIST SP 800-162 ABAC attribute-evaluation framing; OWASP access-control guidance for deny-by-default, fail-secure access control, authorization event logging, and unit/integration tests. | Repo-side closed. This does not prove multi-instance approval-store durability, live policy activation deployment, customer PEP/no-bypass, production readiness, or a line-by-line behavioral audit of every release-policy-control-plane file. |
| OPS-164 approval TTL defaults hardcoded by risk class | `accepted limitation` | P3 | operational boundedness; auditability | OPS-SWEEP-21 confirms approval TTL defaults are hardcoded by risk class: R0/R1/R2 24h, R3 8h, R4 4h; per-request `expiresAt` can override. | Keep; document hardcoding as deliberate and safer than broad environment-wide loosening. |
| OPS-165 policy-control-plane line-by-line audit depth gap | `not proven` | P3 | auditability; no overclaim | OPS-SWEEP-21 audits 18 source files / 6,656 source lines at spec, entry-point, targeted-deep, and test-inventory level; it does not line-by-line behaviorally audit every branch. | Keep the disclosure; future sub-sweeps drill into specific modules only when justified. |
| OPS-166 consequence-admission line-by-line audit depth gap | `not proven` | P2 | auditability; no overclaim | OPS-SWEEP-22 maps 155 source files and 53 admission/consequence-related tests at spec, entry-point, exported-constant, critical-path, route/bootstrap, and inventory depth; it does not line-by-line behaviorally audit every branch in the `policy-foundry-*`, `shadow-*`, `golden-refund-*`, assurance/invariant, action-surface, customer-gate, or test assertion families. | Keep as non-blocking sweep-budget disclosure; spawn focused sub-sweeps only where module-specific risk warrants. |
| OPS-167 caller-supplied `observedFeatures` trust origin | `closed` | P2 | fail-closed boundary; customer authority | OPS-SWEEP-22 confirms `createGenericAdmissionEnvelope` normalizes `observedFeatures`. The runtime now also normalizes `observedFeatureOrigins`, treats `caller-supplied` as untrusted for positive readiness, and holds programmable-money adapter readiness with `adapter-readiness-origin-untrusted` unless `observedFeatures.adapterReady` is paired with `operator-attested`, `customer-gateway`, `attestor-runtime`, or `trusted-adapter`. Generic admissions also normalize `authoritySources` and run `evaluateConsequenceUntrustedContentAuthority` when a domain profile requires authority or authority-source metadata is present: untrusted content, tool output, retrieved content, and model summaries cannot authorize a consequence or self-promote into trusted authority, and trusted authority sources require digest evidence. When an authority source claims `approval`, generic admissions also normalize `approvals` and run `evaluateConsequenceApprovalProvenance`: missing approval provenance holds, and untrusted/model/tool approval claims fail closed. Generic admissions now also normalize structured `toolResults`, `agenticSupplyChain`, `staleAuthorityPolicy`, `decisionContextDrift`, `authorityCreep`, `humanReviewFatigue`, and `multiAgentDelegation` metadata and run the matching guards. Untrusted external tool results used as authority/instruction fail closed; unsafe supply-chain metadata can hold or block; stale policy/authority mismatch can hold or block; missing or drifted decision context can hold or block; authority-creep measurement-as-authority, blocked metric use, lineage binding gaps, audit writes, policy activation, live enforcement activation, or authority-action requests can hold or block; unsafe review-packet posture such as raw payload storage or auto-enforce requests can block; unsafe delegation such as unapproved delegated scope, self-approval, cycles, missing authority/scope, or cross-tenant unscoped chains can hold or block. Only digest/count/boolean evidence is surfaced. Restrictive signals such as `policyBlocked`, `blocked`, `unsafe`, and `narrowRequired` may still make the decision stricter. `docs/01-overview/consequence-admission-quickstart.md`, `docs/01-overview/hosted-action-authorization-api.md`, and `docs/api/attestor-action-authorization.openapi.json` document the contract. Locking tests: `tests/generic-admission-mode-ladder.test.ts`; `tests/generic-admission-routes.test.ts`; `tests/generic-admission-guard-route-matrix.test.ts`; `tests/hosted-action-authorization-openapi.test.ts`; `tests/consequence-admission-proof-discipline.test.ts`. | Repo-side trusted-origin marker is closed for generic adapter readiness, and the untrusted-content authority guard, approval-provenance guard, tool-result poisoning guard, agentic supply-chain guard, stale authority/policy guard, decision-context drift binding, authority-creep guard, human-review fatigue guard, and multi-agent delegation guard are runtime-wired for generic admissions when their structured metadata is in scope. This does not prove live customer/operator/tool-adapter/policy-store/IdP/approval-workflow/reviewer-workflow/assurance-operations/agent-runtime provenance, Customer PEP no-bypass, production readiness, signed-approval PKI coverage or signed tool-result attestation verification through the generic route, semantic validation of arbitrary policy/evidence refs, live reviewer behavior, live policy activation, or live delegation authority. |
| OPS-168 consequence retry-attempt ledger shared-store proof gap | `closed / live-proof-only` | P2 | replay and idempotency safety; auditability | OPS-SWEEP-22 confirms `retry-attempt-ledger.ts` declares `shared-atomic` and references `src/service/consequence-shared-atomic-stores.ts`; shared store schema exists, but `productionSharedRuntimeWired: false` and `rlsForced: false` remain. `LP-CONSEQUENCE-RETRY-ATTEMPT-LEDGER-SHARED-STORE` is now repo-gated by `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF`, and `tests/consequence-admission-proof-discipline.test.ts` locks the no-claim plus gate alignment. | Capture `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF` with shared atomic store runtime, duplicate/idempotency constraints across instances, and RLS-forced tenant-scope evidence before multi-instance retry-budget claims. |
| OPS-169 customer-PEP runtime adoption no-overclaim | `accepted limitation / baseline continuation` | P1 baseline-linked | customer authority; fail-closed boundary; no overclaim | OPS-SWEEP-22 confirms `customer-pep-runtime-adoption.ts` returns `productionReady: false` and `activatesEnforcement: false`, and `protected-admission-e2e-proof-plan.ts` is a proof-plan evaluation only. This continues the standing `Customer PEP no-bypass` P0 live-proof blocker; it is not a new repo-side P1. | Keep `LP-CUSTOMER-PEP-NO-BYPASS` as the closure gate; do not treat repo contracts as live PEP proof. |
| OPS-170 consequence-admission `index.ts` size and barrel surface | `closed` | P3 | auditability; operational boundedness | OPS-SWEEP-22 confirmed the central consequence-admission barrel surface was broad. The implementation-bearing builders remain in `src/consequence-admission/index.ts`, and the broad package-surface re-export catalogue is now isolated in `src/consequence-admission/public-surface.ts`. The OPS-170 guard validates `createConsequenceAdmissionResponse`, `createGenericAdmissionEnvelope`, `evaluateConsequenceAdmissionRetryBudget`, and `retryBudgetInstruction` against existing behavioral coverage for admitted/narrow/invalid response paths, generic admission modes, retry-budget mismatch handling, shared retry-budget evidence, and the split public-surface catalogue. Locking test: `tests/consequence-admission-index-surface.test.ts`. | No repo-side action remains for OPS-170. Future barrel splits are allowed only when they preserve the locked contracts; OPS-171 is accepted as a sub-sweep backlog for family-specific sub-sweeps. |
| OPS-171 consequence-admission family sub-sweep queue | `accepted limitation / sub-sweep backlog` | P2 | auditability; no overclaim | OPS-SWEEP-22 inventories but does not behaviorally audit `policy-foundry-*`, `shadow-*`, `golden-refund-*`, assurance/invariant, action-surface, customer-gate, or the 53 admission/consequence-related test files at assertion depth. `docs/audit/ops-171-consequence-admission-family-sub-sweep-queue.md` now records concrete queue slices, trigger conditions, authority boundaries, and minimum closure evidence. Locking test: `tests/consequence-admission-family-sub-sweep-queue.test.ts`. | Accepted limitation for the current remediation list: do not claim family-level behavioral closure until a queued family sub-sweep is executed with evidence. |
| OPS-172 crypto execution admission semantic policy/evidence boundary | `accepted limitation / customer-proof-only` | P2 | proof integrity; customer authority; no overclaim | `src/crypto-execution-admission/index.ts` keeps release, policy, enforcement, and adapter readiness as required admission inputs; `tests/crypto-execution-admission.test.ts` proves missing release authorization returns `needs-evidence`, not `admit`; `docs/02-architecture/crypto-execution-admission-platform-surface.md` now states that customer adapters must verify arbitrary policy/evidence/receipt references against the active customer store. | Keep the package boundary honest: repo-side plans can require digest-bound handoff artifacts, but do not claim semantic validation of arbitrary live customer policy/evidence refs without a customer adapter/store proof. |
| OPS-173 x402 settlement evidence boundary | `closed repo-side / live-proof-only` | P2 | proof integrity; fail-closed boundary; no overclaim | The x402 adapter already requires settlement success, `PAYMENT-RESPONSE`, transaction, network, payer, and amount binding before `allow`; `tests/crypto-authorization-core-x402-agentic-payment-adapter.test.ts` now locks that a successful settlement response without transaction evidence blocks. `docs/02-architecture/crypto-execution-admission-platform-surface.md` keeps chain receipt / settlement verification as customer-adapter responsibility. | Capture a live x402 facilitator / chain receipt proof before claiming independent settlement verification or production crypto payment readiness. |
| OPS-175 crypto pack hosted-route disclosure | `closed / accepted limitation` | P3 | no overclaim; customer authority | `cryptoExecutionAdmissionPublicSurface()` now exposes `hostedRouteClaimed: false`, `customerSideIntegrationRequired: true`, and `attestorWalletCustodySignerClaimed: false`; `tests/crypto-execution-admission-platform-surface.test.ts` locks those flags. Product packaging and platform-surface docs now state that crypto packs are customer-side package boundaries, not hosted `/api/v1/crypto/*` routes. | Keep `publicHostedCryptoRouteClaimed: false`; do not add a hosted crypto route or wallet/custody/signer claim without a new design review and customer proof plan. |
| Customer PEP no-bypass | `needs live test` | P0 | enforcement boundary; customer authority | Baseline requires live customer PEP integration proof; repo-side primitives are not enough. Test contract: `tests/live-customer-pep-no-bypass.test.ts`. | Build one reference PEP deployment and prove direct downstream bypass fails. |
| External KMS runtime signing | `needs ops proof` | P0 | proof integrity; key authority | Baseline requires KMS/HSM-backed signer proof; repo has PKI PVC boundary hardening but not live KMS authority proof. Test contract: `tests/live-kms-runtime-signing.test.ts`. | Wire and prove external KMS runtime signing before limited enforcement. |
| Shared replay / introspection store | `needs live test` | P0/P1 | replay and idempotency safety | Repo has replay/freshness primitives and live proof gates; HA behavior needs Redis/Postgres-backed live proof. Test contract: `tests/live-shared-replay-introspection-store.test.ts`. | Run multi-instance replay, outage, and fail-closed tests. |
| `ops/**` audit gap | `partial / live-proof-only` | P0/P1 | runtime readiness | `OPS-SWEEP-01` through `OPS-SWEEP-22` are report-indexed; high-signal live-proof rows below keep Sweep 01-08 repo-side closure separate from live cloud/IAM/KMS/network/storage/provider proof. | Continue ops sweep with remaining provider/live proof surfaces and capture live artifacts before stronger claims. |
| OPS-01 active base Gateway is HTTP bootstrap | `closed repo-side / live-proof-only` | P0 | runtime readiness; data minimization | The base Gateway remains HTTP bootstrap, but `scripts/render-ha-release-bundle.ts` rewrites production HA bundles to the HTTPS listener and `scripts/check/check-ops-live-shadow-readiness.mjs` requires `ATTESTOR_LIVE_SHADOW_HTTPS_PROOF`. Locking test: `tests/ha-release-bundle-render.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-HTTPS-EDGE`; do not claim live-shadow HTTPS until the rendered bundle is applied and probed. |
| OPS-02 ClusterSecretStore backend proof | `closed repo-side / live-proof-only` | P0 | runtime readiness; data minimization | `ops/kubernetes/ha/providers/external-secrets/clustersecretstore.gke.example.yaml` documents the GCP Secret Manager / Workload Identity shape and the live gate requires `ATTESTOR_CLUSTER_SECRET_STORE_PROOF`. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-CLUSTER-SECRET-STORE`; repository examples do not prove live Secret Manager access or ExternalSecret sync. |
| OPS-03 worker pod default ServiceAccount | `closed` | P1 | runtime readiness; least privilege | `ops/kubernetes/ha/worker-serviceaccount.yaml` defines `attestor-worker-runtime` with `automountServiceAccountToken: false`, and the worker deployment uses that ServiceAccount. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | No repo action unless the worker identity model changes; cloud IAM least-privilege remains `LP-GCP-IAM-LEAST-PRIVILEGE`. |
| OPS-04 missing HA NetworkPolicy | `closed / live-proof-only` | P0 | runtime readiness; operational boundedness | `ops/kubernetes/ha/networkpolicy.yaml` is part of the HA kustomization and defines default-deny plus explicit runtime allowlists; the live gate requires `ATTESTOR_NETWORK_POLICY_PROOF`. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-NETWORK-POLICY`; YAML does not prove live CNI enforcement or managed-backend egress behavior. |
| OPS-05 Cloud Armor / WAF referenced by repo only | `closed / live-proof-only` | P1 | runtime readiness; operational boundedness | `ops/kubernetes/ha/providers/gke/gcpbackendpolicy.yaml` references the edge policy and provider overlays/docs name WAF proof gates; `scripts/check/check-ops-live-shadow-readiness.mjs` requires `ATTESTOR_EDGE_WAF_PROOF`. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-EDGE-WAF`; repository policy references do not prove live Cloud Armor / AWS WAF attachment or rule behavior. |
| OPS-06 static image tags in live HA promotion path | `disputed/closed` | P1 | release provenance; runtime readiness | `scripts/probe/probe-ha-release-inputs.ts` and `scripts/render-ha-release-bundle.ts` reject tag-only production HA image refs; static bootstrap manifests remain examples rather than a live-shadow release contract. | Keep production HA promotion on immutable digest refs; do not treat bootstrap examples as live release evidence. |
| OPS-07 hosted MFA encryption key optional in HA API manifest | `closed` | P1 | data minimization and redaction; runtime readiness | `ops/kubernetes/ha/api-deployment.yaml` no longer marks the MFA encryption key secret optional, and the ops live-shadow repo gate verifies the invariant. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Live secret backend population remains part of `LP-CLUSTER-SECRET-STORE`, not a repo-closed fact. |
| OPS-17 release-runtime PKI PVC storage boundary | `closed / live-proof-only` | P1 | proof integrity; runtime readiness | `ops/kubernetes/ha/release-runtime-pki-pvc.yaml` uses an explicit storage boundary label, `storageClassName`, RWX semantics, and proof-gate annotation; HA probes/renderers require `ATTESTOR_RELEASE_RUNTIME_PKI_STORAGE_CLASS`. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ha-release-input-probe.test.ts`, `tests/ha-release-bundle-render.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-RUNTIME-PKI-STORAGE`; repository PVC shape does not prove encryption, backup, RWX behavior, or non-exportability in the target cluster. |
| OPS-18 parallel TLS material paths | `closed / live-proof-only` | P1 | runtime readiness; operational boundedness | The cert-manager and External Secrets TLS overlay READMEs forbid applying both writers to `attestor-tls`, and the live gate requires `ATTESTOR_TLS_MATERIAL_SOURCE_PROOF`. Locking test: `tests/kubernetes-ha-bundle.test.ts`, `tests/ops-live-shadow-readiness.test.ts`. | Capture `LP-TLS-MATERIAL-SOURCE`; repository docs cannot prove which overlay the target cluster applied. |
| OPS-19/20/24 cert-manager placeholder and issuer ownership | `accepted limitation / live-proof-only` | P2 | runtime readiness; no overclaim | `docs/audit/ops-sweep-02-pki-tls-secrets-remediation.md` records cert-manager issuer/hostname/email examples as environment-owned placeholders, not deployable live proof. | Keep examples as examples; rendered/applied manifests must be verified before live-shadow TLS claims. |
| OPS-21 wildcard TCP 443 egress | `accepted limitation / live-proof-only` | P2 | operational boundedness; no overclaim | HA docs keep external HTTPS egress explicit for provider APIs and require stronger service-mesh/egress-gateway proof under `ATTESTOR_NETWORK_POLICY_PROOF` for higher-risk deployments. | Do not claim domain-level egress control from Kubernetes NetworkPolicy alone. |
| OPS-22 Cloud Armor policy referenced by name only | `closed / live-proof-only` | P2 | runtime readiness; operational boundedness | The GKE backend policy reference is repo-present and live-shadow proof-gated through `ATTESTOR_EDGE_WAF_PROOF`. | Capture `LP-EDGE-WAF`; policy rules and attachment are live project state. |
| OPS-23 ExternalSecret refresh interval | `accepted limitation` | P3 | runtime readiness; operational boundedness | External Secrets docs make refresh interval tuning explicit via `ATTESTOR_HA_EXTERNAL_SECRET_REFRESH_INTERVAL`, and HA probes validate interval syntax. | Keep rotation runbooks aligned with the configured refresh interval. |
| OPS-25 Alertmanager receivers empty | `live-proof-only` | P0 | runtime readiness; operational boundedness | `ops/observability/alertmanager/alertmanager.yml`; `scripts/render-alertmanager-config.mjs`; `docs/audit/ops-sweep-03-observability-remediation.md`. | Capture real alert delivery proof before live shadow. |
| OPS-26 Prometheus metrics token literal | `closed` | P0 | data minimization and redaction; operational boundedness | `ops/observability/prometheus/prometheus.yml` uses `credentials_file`; credential renderer emits `prometheus-metrics-token`. Locking test: `tests/observability-bundle.test.ts`, `tests/observability-credentials-render.test.ts`, `tests/ops-sweep-03-observability-remediation.test.ts`. | Keep token rotation in live proof / secret backend process. |
| OPS-27 Loki unauthenticated local API | `live-proof-only` | P1 | data minimization and redaction; tenant isolation | Local Loki has `auth_enabled: true`; OTel/Grafana use `X-Scope-OrgID`. | Prove managed backend or live Loki auth/network boundary. |
| OPS-30 security event alert coverage | `closed` | P1 | runtime readiness; auditability | `ops/observability/prometheus/alerts.yml` includes admin auth failure, rejection spike, webhook signature failure, and budget telemetry alerts. Locking test: `tests/observability-bundle.test.ts`, `tests/ops-sweep-03-observability-remediation.test.ts`. | Add per-tenant variants when live metrics carry safe tenant labels. |
| OPS-38 OTel collector mutable image tag | `stale/closed` | P0 | release provenance; runtime readiness | Current `ops/kubernetes/observability/deployment.yaml` uses `otel/opentelemetry-collector-contrib:0.152.0@sha256:...`; `tests/kubernetes-observability-bundle.test.ts` locks this. Locking test: `tests/kubernetes-observability-bundle.test.ts`, `tests/ops-sweep-04-storage-collector-remediation.test.ts`. | No repo action unless image policy changes; live admission/pull policy remains ops proof. |
| OPS-39 Redis recovery auth | `closed / live-proof-only` | P1 | replay and idempotency safety; operational boundedness | `ops/redis/redis-recovery.conf` uses protected mode, ACL file, default-user-off compose generation, and disabled high-risk Redis commands. Locking test: `tests/ops-sweep-04-storage-collector-remediation.test.ts`, `tests/dr-bundle.test.ts`. | Prove live Redis network isolation, ACL policy, and secret rotation. |
| OPS-40 PostgreSQL PITR local WAL archive | `partial / live-proof-only` | P1 | runtime readiness; operational boundedness | `ops/postgres/pitr/archive-wal.sh` supports fail-closed offsite WAL mirroring and SHA-256 sidecars; restore verifies checksums. | Capture base backup, offsite object-store, encryption, and restore-drill proof. |
| OPS-41 OTel collector securityContext | `closed` | P1 | runtime readiness; operational boundedness | `ops/kubernetes/observability/deployment.yaml` adds non-root, RuntimeDefault seccomp, no privilege escalation, read-only root filesystem, and dropped capabilities. Locking test: `tests/kubernetes-observability-bundle.test.ts`, `tests/ops-sweep-04-storage-collector-remediation.test.ts`. | Prove live Pod Security Admission/runtime behavior if making production claims. |
| OPS-44 observability backend namespace mismatch | `closed for base bundle / live-proof-only` | P2 | runtime readiness; auditability | Base OTel deployment now points at Tempo/Loki service names in `attestor-observability`; README documents explicit override proof for shared monitoring namespaces. | Include any namespace override in live proof. |
| OPS-54 observability namespace NetworkPolicy | `closed / live-proof-only` | P2 | runtime readiness; operational boundedness | `ops/kubernetes/observability/networkpolicy.yaml` adds default-deny and explicit OTLP, metrics, DNS, local backend, and HTTPS egress allowlists; `scripts/check/check-ops-live-shadow-readiness.mjs` and `tests/kubernetes-observability-bundle.test.ts` lock the repo evidence. | Prove live CNI NetworkPolicy enforcement and any managed-backend egress override before stronger live-shadow claims. |
| OPS-55 collector Workload Identity annotation | `disputed/closed` | P2 | runtime readiness; least privilege | Base collector uses the Kubernetes attributes processor with `auth_type: serviceAccount`, read-only Kubernetes metadata RBAC, and no direct GCP API access; External Secrets Workload Identity remains on the secret-store/controller path, not this ServiceAccount. | Do not add a cloud IAM binding to this collector ServiceAccount unless a future collector path actually calls cloud APIs. |
| OPS-56 observability namespace Pod Security Admission labels | `closed / live-proof-only` | P2 | runtime readiness; operational boundedness | `ops/kubernetes/observability/namespace.yaml` now labels the namespace for restricted Pod Security Admission enforce/audit/warn; collector manifest already satisfies non-root/seccomp/no-privilege-escalation/read-only-rootfs/drop-all requirements. | Prove live Pod Security Admission behavior in the target cluster before stronger runtime claims. |
| `src/service` admin routes gap | `partial / live-proof-only` | P0/P1 | customer authority; service/API security | OPS-SWEEP-06 maps all 32 `registerAdminRoutes` routes, closes repo-side P1/P2 admin route findings, and records accepted limitations for admin reads and one-time key material. | Keep live role-key/rate-limit proof in `live-proof-register.md`; continue public-route and live probe work separately. |
| Required commit signatures | `closed repo-side / live-proof-only` | P0/P1 | release provenance | Baseline branch-protection gap is localized as OPS-121; repo-side workflow assertion and live-proof gate now exist, but the GitHub setting and unsigned-commit negative proof remain live evidence. Locking test: `tests/branch-governance-policy.test.ts`. | Capture `LP-BRANCH-PROTECTION-SIGNATURES`. |
| Required PR reviews | `closed repo-side / live-proof-only` | P0/P1 | release provenance; auditability | Baseline branch-protection gap is localized as OPS-122; repo-side workflow assertion and live-proof gate now exist, but the GitHub settings and same-author/self-merge negative proof remain live evidence. Locking test: `tests/branch-governance-policy.test.ts`. | Capture `LP-BRANCH-PROTECTION-REVIEWS`. |
| Test adequacy map | `closed` | P1 | auditability; no overclaim | `scripts/check/check-finding-test-coverage.mjs` and `tests/finding-test-coverage.test.ts` now machine-check closed P0/P1 rows for structured `Locking test:` markers and existing `tests/*.test.ts` paths; P0/P1 live/ops-proof blockers name future `Test contract:` paths; OPS-137 is stale/closed by existing renderer-output tests. Locking test: `tests/finding-test-coverage.test.ts`. | Keep the guard green before closing future P0/P1 rows; live-proof-only blockers still require live/operator evidence. |

## Intake Rule For New Findings

Every P0/P1 finding and every finding that changes public readiness posture must
be added here with:

- current state,
- severity,
- protected principle,
- current repository evidence or explicit live-proof gap,
- required action.

Low/Medium findings may remain in a report until they affect baseline posture,
public claims, or a remediation PR.

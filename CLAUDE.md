@AGENTS.md

## Claude Code

Keep this bridge short. The root `AGENTS.md` is the shared source for Attestor agent behavior. Use the playbooks under `docs/ai/playbooks/` only when the task matches that workflow.

SPECIFICATION Spec

CONSTANTS
  Tenants = {tenantA, tenantB}
  Requests = {requestA, requestB}
  Digests = {digestA, digestB, noDigest}
  Nonces = {nonceA, nonceB}
  NoDigest = noDigest
  NoNonce = noNonce

INVARIANT TypeOK
INVARIANT NoAdmitWithoutAuthority
INVARIANT NoEnforcementWithoutPacket
INVARIANT NoCrossTenantLeak
INVARIANT NoReviewBypass
INVARIANT MonotoneFusion
INVARIANT ReplaySafety

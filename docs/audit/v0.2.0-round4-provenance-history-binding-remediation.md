# v0.2.0 Round 4 Provenance And History Binding Remediation

Status: remediation record for validated Round 4 findings against
`origin/master` at `b79e0309d75b6787e540fbb3a63e6eaf5cd485c9`.

This record is not a full audit, not a production-readiness claim, and not a
claim that every Round 4 finding is fixed. It records the PR-sized slice that
was validated and remediated.

## Audit Run Header

```text
Audit ID: v0.2.0-round4-provenance-history-binding-remediation
Mode: remediation
Target ref: origin/master b79e0309d75b6787e540fbb3a63e6eaf5cd485c9
Date: 2026-05-19
Reviewer: Codex
Threat model / framework: project-owner supplied Round 4 audit report; GitHub Actions supply-chain hardening; packet/history proof binding
Research anchors: GitHub Actions security hardening guidance; OpenSSF Scorecard pinned-dependencies; OWASP input validation
Scope: workflow install-script hardening; signed assurance packet history-verification binding
Out of scope: complete repository audit, production deployment validation, customer PEP behavior, external immutable log integration
Known limitations: this is a targeted remediation slice. It does not close every audit finding or establish enterprise production readiness.
```

## Findings In This Slice

| ID | Status | Severity | Protected principle | Repository evidence | Smallest safe fix | Regression evidence |
|---|---|---:|---|---|---|---|
| B-032 | mitigated before this PR | high | release provenance, auditability | Current `origin/master` already pins GitHub Actions in `.github/workflows/release-provenance.yml` by full commit SHA and includes artifact + SBOM attestation steps | No code change in this PR; retain status as already mitigated | `rg -n "uses: .+@v[0-9]" .github/workflows` has no matches |
| B-031 | mitigated before this PR | medium | release provenance, runtime readiness | Current `origin/master` already pins GitHub Actions in `.github/workflows/production-rehearsal.yml` by full commit SHA | No pinning change in this PR; retain status as already mitigated | workflow inspection |
| B-030 | confirmed / fixed in PR | low | supply-chain hardening, operational boundedness | Workflow install steps used plain `npm ci`; the Dockerfile already used `npm ci --ignore-scripts --no-audit --no-fund` | Align workflow install steps with the existing no-install-scripts pattern | GitHub CI plus workflow diff |
| C-014 | confirmed / fixed in PR | medium | proof integrity, auditability | `SignedAssurancePacketHistoryBinding.verified` was caller-supplied and packet creation did not require a matching `ConsequenceTamperEvidentHistoryVerification` result | Require matching tamper-history verification evidence when `historyBinding.verified` is true; check root digest, last-entry digest, entry count, and verification digest | `tests/signed-assurance-packet.test.ts`; related downstream packet consumer tests |

## Validated But Not Treated As Complete Audit

The Round 4 report's statement that "the audit is complete" is explicitly not
accepted here. This remediation only covers the workflow/provenance and packet
history-binding slice. Service routes, API/provider integrations, signing
implementation depth, release enforcement, domain packs, ops manifests, and
test coverage adequacy still require separate audit passes.

## Verification Plan

```text
npm run test:signed-assurance-packet
npm run test:consequence-tamper-evident-history
npm run test:authority-creep-guard
npm run test:assurance-measurement-plane
npm run test:outcome-incident-feedback-contract
npm run test:outcome-feedback-coe-wiring
npm run test:shadow-runtime-pipeline
npm run test:consequence-admission-package-surface
npm run test:production-rehearsal-workflow
npm run test:package-script-runner
npm run test:container-security-baseline
npm run security:supply-chain-baseline
npm run security:audit-high
npm run typecheck
npm run typecheck:hygiene
```

Full `npm run verify` is intentionally not required for this narrow remediation
slice unless CI or reviewer evidence shows broader impact.

# v0.2.0 Round 10 Signature And Provenance Verification

Status: remediation and verification record for two Round 10 high-priority
audit items against `origin/master` at
`743dc11617c041abbb843827741a5918e7bc1f9b`.

This record is not a complete Round 10 audit, not a production-readiness claim,
and not a claim that every signing or release-provenance path is audited. It
records one PR-sized verification/remediation slice.

## Audit Run Header

```text
Audit ID: v0.2.0-round10-signature-provenance-verification
Mode: remediation / verification
Target ref: origin/master 743dc11617c041abbb843827741a5918e7bc1f9b
Date: 2026-05-19
Reviewer: Codex
Threat model / framework: assurance packet signature self-attestation; release provenance workflow action immutability
Research anchors: Node.js crypto Ed25519 sign/verify API; RFC 8032 Ed25519 verification semantics; GitHub Actions secure-use guidance for full-length SHA action pinning
Scope: `ed25519` signed assurance packet verification and `release-provenance.yml` action pin verification
Out of scope: external KMS/HSM signature verification, production signer distribution, production release readiness, complete signing subsystem audit, complete CI audit
Known limitations: external KMS/HSM packet signatures remain boundary metadata in this contract; B-019 productionReady self-attestation is not closed by this slice.
```

## Findings In This Slice

| ID | Status | Severity | Protected principle | Repository evidence | Smallest safe fix / verification | Regression evidence |
|---|---|---:|---|---|---|---|
| R3 B-018 | accepted / fixed for `ed25519` packet signatures | high | proof integrity, auditability | `createSignedAssurancePacket()` normalized an `ed25519` signature record without verifying the canonical signing payload against a public key | Require `signatureVerificationPublicKeyPem` for `ed25519`, verify the signature over the canonical signing payload, and require the signature fingerprint to match the verification key | `tests/signed-assurance-packet.test.ts` |
| R4 B-032 | verified closed | high | release provenance | `.github/workflows/release-provenance.yml` now uses full-length commit SHAs for checkout, setup-node, upload-artifact, and attest actions | No code change needed in this slice; workflow inspection verifies the actions are SHA-pinned | `rg -n "uses:" .github/workflows/release-provenance.yml` |

## Re-Audit Notes

The signed assurance packet remains a no-authority artifact:
`canAdmit = false`, `activatesEnforcement = false`, and `autoEnforce = false`.
This fix closes the local `ed25519` self-attestation path only. It deliberately
does not claim that external KMS/HSM signatures are locally verified by this
module, and it does not close the separate production-boundary question tracked
as B-019.

## Verification Plan

```text
npm run test:signed-assurance-packet
npm run typecheck
npm run typecheck:hygiene
```

# v0.2.0 Round 2 Runtime Invariant Remediation

Status: remediation record for validated Round 2 findings against
`origin/master` at `ac7d07a53831a364ede3c1ec8f545d98647ce884`.

This record is not an audit certification, not a production-readiness claim,
and not a claim that every Round 2 finding is fixed. It documents the
PR-sized slice that was validated and remediated.

## Audit Run Header

```text
Audit ID: v0.2.0-round2-runtime-invariant-remediation
Audit title: LayerOpinion runtime invariant and canonical ref ordering remediation
Mode: remediation
Target ref: origin/master ac7d07a53831a364ede3c1ec8f545d98647ce884
Date: 2026-05-19
Reviewer: Codex
Threat model / framework: project-owner supplied Round 2 audit report; OWASP input validation; RFC 8785 canonicalization constraints
Research anchors: OWASP Input Validation Cheat Sheet; RFC 8785 JSON Canonicalization Scheme
Scope: LayerOpinion runtime invariants before fusion; canonical shadow event reference ordering
Files/modules inspected: layer-opinion-schema.ts; relationship-aware-monotone-fusion.ts; canonical-shadow-event-schema.ts; related tests and architecture docs
Out of scope: source-system proof-of-fetch, global producer allowlisting, customer-gate architecture, signed assurance packet validation, live deployment
Known limitations: this is the first runtime-boundary validator slice, not a universal validator for every consequence-admission contract
```

## Findings In This Slice

| ID | Status | Severity | Protected principle | Repository evidence | Smallest safe fix | Regression evidence |
|---|---|---:|---|---|---|---|
| B-014 / C-008 | confirmed / fixed in PR | high | proof integrity, fail-closed boundary | `LayerOpinion` declared `beliefMassConservationRequired: true`, while fusion consumed `opinion.beliefMass.hazard` without a runtime conservation check | Add `assertLayerOpinionRuntimeInvariants()` and call it before relationship-aware monotone fusion scores opinions | `tests/layer-opinion-schema.test.ts`; `tests/relationship-aware-monotone-fusion.test.ts` |
| M-001 scoped slice | confirmed / partial | high | auditability, proof integrity | TypeScript literals protected the schema shape, but forged runtime values could cross module boundaries if cast or parsed | Validate critical LayerOpinion flags, envelope binding, digest refs, reason-code shape, category/kind binding, numeric ranges, and belief mass conservation at the fusion boundary | targeted LayerOpinion and fusion regression tests |
| C-010 | confirmed / fixed in PR | medium | replay and idempotency safety, auditability | Canonical shadow event ref arrays preserved caller order, so semantically equivalent refs could hash differently | Sort normalized reference arrays by `kind`, `digest`, and `origin` before canonical hashing | `tests/shadow-event-canonical-schema.test.ts` |

## Validated But Not Remediated In This Slice

| ID | Status | Rationale |
|---|---|---|
| B-007 | disputed as stated | RFC 8785 does not require Unicode NFC normalization of JSON string data; changing string data during canonicalization would be a separate breaking semantic choice. |
| B-010 | not remediated | `producer` is intentionally evaluated by later evidence-model trusted-producer policy, not a global constructor allowlist. |
| B-011 | accepted limitation | Digest format validation is not source-system proof-of-fetch. That proof belongs to source verification / evidence fetch layers, not the canonical event constructor. |
| B-012 / B-013 | partial | This slice validates LayerOpinion reason-code shape at the fusion boundary. A full cross-contract reason-code registry remains separate work. |
| B-017 | not remediated | Source-plane privilege checks require a separate authority-boundary design pass. |

## Verification Plan

```text
npm run test:layer-opinion-schema
npm run test:relationship-aware-monotone-fusion
npm run test:shadow-event-canonical-schema
npm run test:conflict-abstention-gate
npm run test:shadow-runtime-pipeline
npm run test:shadow-runtime-activation-runner
npm run test:golden-refund-runtime-smoke
npm run test:golden-refund-engine-visibility
npm run test:golden-refund-demo
npm run demo:golden-refund -- --json
npm run typecheck
npm run typecheck:hygiene
```

Full `npm run verify` is intentionally not required for this narrow remediation
slice unless CI or reviewer evidence shows broader impact.

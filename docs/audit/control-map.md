# Audit Control Map

This map connects Attestor's current audit evidence to external control
anchors. The anchors support control mapping only. They are not certifications
or independent assurance claims.

## External Anchors

| Anchor | How it is used | No-overclaim boundary |
|---|---|---|
| NIST SP 800-218 Secure Software Development Framework | Secure SDLC, provenance, vulnerability response, recurrence prevention, and release evidence discipline. | Does not prove SSDF conformance or government certification. |
| NIST Cybersecurity Framework 2.0 | Govern / Identify / Protect / Detect / Respond / Recover vocabulary for posture and roadmap planning. | Does not prove organizational CSF maturity. |
| OWASP SAMM | Program-level governance, implementation, verification, operations, and measurement structure. | Does not prove SAMM maturity level. |
| OWASP ASVS | Application security verification requirement vocabulary for API, auth, session, logging, and crypto controls. | Does not prove full ASVS verification. |
| CWE | Weakness taxonomy for specific finding mapping. | Does not prove exhaustive weakness coverage. |

## Current Control Areas

| Control area | Protected principle | Current repo evidence | Live proof needed | Next action |
|---|---|---|---|---|
| Current posture governance | no overclaim; auditability | `docs/audit/current-posture-baseline.md`; this evidence system. | None for repository claims; live claims still need proof. | Keep baseline updated only on material P0/P1 or readiness changes. |
| Finding lifecycle closure | auditability; no overclaim | `docs/audit/finding-lifecycle-and-evidence-ledger.md`; `scripts/check/check-audit-finding-evidence.mjs`; `scripts/check/check-finding-test-coverage.mjs`; `tests/finding-test-coverage.test.ts`; `docs/audit/ledger-template.md`; OPS-SWEEP-17 P0/P1 finding-to-test matrix. | None for repository lifecycle schema; live/ops blockers still require the live proof register before runtime or production claims. | Keep `test:audit-finding-test-coverage` green so new closed P0/P1 findings are mapped to tests through the machine-checked `Locking test:` marker. |
| Report/finding indexing | auditability; no overclaim | `docs/audit/report-index.md`; `docs/audit/finding-index.md`; `scripts/check/check-security-evidence-system.mjs`. | None for index presence. | Backfill high-signal historical P0/P1 findings as they are revalidated. |
| Typed financial evidence contracts | proof integrity; auditability; no overclaim | OPS-185 repo-side hardening compares financial contract column type metadata, validates row values against declared scalar types, and fails numeric constraints/control totals on missing, non-numeric, or empty numeric input. | Live connector schema/output proof remains required before live financial filing, production, or compliance claims. | Keep `tests/financial-data-contracts.test.ts` and `tests/financial.test.ts` green before stronger financial evidence claims. |
| Workload-bound release proof source | fail-closed boundary; proof integrity; customer authority; replay and idempotency safety | OPS-186 repo-side hardening requires trusted expected certificate or SPIFFE binding for mTLS/SPIFFE-bound offline release presentations and fails closed with `missing-workload-proof` when only caller-presented metadata is available. | Live TLS/SPIFFE extraction, Envoy/Istio route-map deployment, shared replay/introspection, and customer PEP no-bypass proof remain required. | Pass trusted bindings from the customer-owned or proxy-owned enforcement point; do not treat request-body proof metadata as workload possession. |
| Account MFA enrollment freshness | customer authority; replay and idempotency safety; auditability | OPS-187 repo-side hardening makes TOTP pending enrollment TTL-bound, requires current password confirmation, clears expired pending secrets, and records bounded audit evidence. | Deployed account-route probes and shared auth-abuse-store behavior remain live-proof-only. | Keep account-route tests green and capture live probes before production account-security claims. |
| Release-token exchange parent liveness | proof integrity; fail-closed boundary; replay and idempotency safety | OPS-188 repo-side hardening requires active-state parent introspection and child registration store for R3/R4 or `introspection_required` token exchange, with bounded result metadata for whether parent introspection was checked. | Live shared introspection/replay proof and customer PEP no-bypass proof remain required. | Keep token-exchange tests green and do not treat cryptographic validity alone as sufficient high-risk exchange authority. |
| Release-token registry lifecycle finality | proof integrity; replay and idempotency safety; auditability | OPS-191 repo-side hardening makes release-token registration insert-only, allows only identical issued-record idempotent replay, rejects duplicate revoked/consumed/expired/different token IDs, and denies duplicate token-exchange child token IDs. | Live shared introspection/replay proof and customer PEP no-bypass proof remain required. | Keep release introspection, shared introspection store, and token-exchange lifecycle tests green before stronger token-lifecycle claims. |
| Required sender-constraint enforcement | fail-closed boundary; proof integrity; customer authority; replay and idempotency safety | OPS-192 repo-side hardening makes the core offline verifier reject non-sender-constrained presentation modes whenever the resolved profile requires sender constraint; online verification inherits the failure before active-state introspection. | Live TLS/SPIFFE extraction, Envoy/Istio route-map proof, shared replay/introspection proof, and customer PEP no-bypass proof remain required. | Keep verifier/workload-binding tests green and do not treat parser-supported bearer modes as acceptable for required-sender profiles. |
| Token-exchange actor proof boundary | customer authority; auditability; proof integrity | OPS-193 repo-side hardening separates request-body actor metadata from trusted actor source. `requireActor` now needs `trustedActor` or an existing signed parent actor chain; request/trusted actor mismatches fail closed. | Live account-session, mTLS/SPIFFE, actor-token, or trusted exchange-gateway extraction proof remains required before stronger live delegation claims. | Do not treat request-body actor labels as delegation authority; raw external actor-token validation remains unsupported. |
| Generic middleware binding derivation | fail-closed boundary; proof integrity; customer authority; replay and idempotency safety | OPS-194 repo-side hardening requires trusted server-side resolvers for default middleware target/output/consequence/body binding. Header-derived binding is explicit `trusted-upstream` mode only, and high-risk bearer-only middleware paths fail closed under required sender constraints. | Live customer PEP no-bypass, upstream gateway signature/body digest proof, live workload extraction, and shared replay/introspection proof remain required. | Keep `tests/release-enforcement-plane-middleware.test.ts` green and do not treat caller-supplied binding headers as authority. |
| Finance filing readiness release gate | fail-closed boundary; proof integrity; customer authority; no overclaim | OPS-195 repo-side hardening makes `internal_report_ready` with zero total/blocking gaps the only finance filing readiness state that can accept the R4 filing release bridge. | Live filing connector evidence, live sender confirmation, customer PEP no-bypass, and compliance/production proof remain required. | Keep finance release tests green and do not issue filing release tokens for `filing_not_ready` or `review_ready`. |
| Declared adapter evidence metadata | proof integrity; auditability; no overclaim | OPS-196 repo-side hardening labels action preconditions and communication attachments as declared metadata in canonical payloads and requires `sha256:<64 lowercase hex>` digest references. | External evidence-store, approval-workflow, and attachment-byte verification remain runtime integration work. | Keep action/communication gateway tests green and do not claim semantic evidence verification from canonical declaration binding alone. |
| Release-review credential-bound authority | customer authority; proof integrity; auditability | OPS-197 and OPS-198 repo-side hardening derive reviewer and override requester identity from the authenticated role-scoped admin credential on token-issuing release-review routes. Body reviewer/requester fields no longer create reviewer or emergency authority. | Live role-key deployment, independent human-review workflow proof, enterprise IdP proof, external incident workflow proof, and production readiness remain required. | Keep `tests/release-review-admin-routes.test.ts` green and do not treat request-body reviewer/requester labels as authority. |
| Release evidence artifact reference boundary | proof integrity; auditability; no overclaim | OPS-199 repo-side hardening labels evidence-pack artifacts as `issuer-derived` or `declared-unverified`, requires strict `sha256:<64 lowercase hex>` declared artifact digests, and keeps DSSE validity separate from external artifact resolution. | External artifact-store retrieval, byte availability, and digest-match proof remain live/runtime integration work. | Keep evidence-pack tests green and do not claim external artifact verification from signed reference inclusion alone. |
| Webhook break-glass grant consumption | replay and idempotency safety; fail-closed boundary; auditability | OPS-200 repo-side hardening requires a `consumeBreakGlassGrant` hook before webhook break-glass admission. Raw timestamp-valid grants remain rejected unless an integration consumes a grant for the evaluated request. | Live shared degraded-mode grant store proof, multi-instance use-budget behavior, customer PEP no-bypass, and production readiness remain required. | Keep webhook and degraded-mode tests green; wire production receivers to shared grant-store consumption before stronger emergency-admission claims. |
| Release-review terminal transition discipline | customer authority; replay and idempotency safety; proof integrity; auditability | OPS-201 repo-side hardening commits pending release-review transitions through `commitPendingTransition(...)` before token/evidence side effects. Stores compare pending status, authority state, and reviewer-decision count so stale final approvals fail before issuing authority material. | Live role-key deployment, live shared-store behavior, customer PEP no-bypass, and full distributed transaction proof across external stores remain required. | Keep release-review route and reviewer-queue tests green; do not claim live multi-store transaction proof from repository CAS checks. |
| Release evidence-pack immutable store handle | proof integrity; auditability; no overclaim | OPS-202 repo-side hardening makes release evidence-pack stores insert-only by pack ID. Same ID plus same bundle digest is idempotent replay; same ID plus different bundle digest fails closed. | External artifact-store retrieval, byte availability, WORM/SIEM storage, production retention, and compliance evidence remain live/operator work. | Keep evidence-pack kernel and shared-store tests green; do not claim external artifact availability from immutable local/shared pack storage. |
| Envoy ext_authz target binding source | fail-closed boundary; customer authority; proof integrity; auditability | OPS-203 repo-side hardening ignores client `attestor-target-id` by default. Target IDs come from server options, route context extensions, proxy-observed destination service, or host/path fallback unless an embedding explicitly opts into client-header labels. | Live Envoy/Istio route-map deployment, workload-binding extraction, customer PEP no-bypass, and production readiness remain required. | Keep Envoy ext_authz tests green and treat client target headers as advisory only unless an integration explicitly accepts that boundary. |
| Generic middleware protected method defaults | fail-closed boundary; customer authority; proof integrity; replay and idempotency safety; auditability | OPS-204 repo-side hardening protects `GET` and `HEAD` by default in the generic HTTP release middleware. Explicit mutation-only route groups can still configure `protectedMethods`, but the default no longer skips read/export paths. | Live customer PEP no-bypass, deployed route-map coverage, upstream gateway trust, and shared replay/introspection proof remain required. | Keep middleware tests green and require explicit opt-out when a route group intentionally skips read methods. |
| Policy discovery emergency-freeze precedence | fail-closed boundary; customer authority; proof integrity; auditability | OPS-205 repo-side hardening checks matching frozen activations before static metadata bundle resolution. Static discovery remains available when no freeze matches, while emergency freeze returns `policy-scope-frozen` through the runtime resolver. | Live policy-control shared-store behavior, deployed emergency procedure proof, role-key deployment, operator rehearsal, and production readiness remain required. | Keep discovery/resolver tests green and do not claim live incident-response behavior from repository precedence alone. |
| Policy lifecycle atomic transition | fail-closed boundary; customer authority; proof integrity; replay and idempotency safety; auditability | OPS-206 repo-side hardening adds a shared-store lifecycle mutation that snapshots policy state, applies activation/freeze/rollback, writes applied and historical records, publishes metadata, and appends policy mutation audit under one transaction-scoped lock set. | Live shared policy-control store deployment, emergency procedure rehearsal, role-key deployment, customer PEP no-bypass, and production readiness remain required. | Keep shared policy-authority and admin-route tests green; do not claim live incident-response closure from repository transaction behavior alone. |
| Shadow policy-candidate status actor provenance | customer authority; proof integrity; auditability; no overclaim | OPS-207 repo-side hardening derives hosted shadow policy-candidate status actors from authenticated account/session context when available, otherwise from the current tenant-auth context. The derived actor is part of status-transition idempotency semantics. Body `actorRef` is metadata only and is not persisted as approval authority. | Live account/session/key deployment, live shadow mutation audit-chain proof, and any independently authenticated customer approval workflow proof remain required. | Keep shadow candidate approval and promotion-draft tests green; do not treat request-body actor labels as approval authority. |
| Shadow policy-candidate digest-bound approval | proof integrity; customer authority; replay and idempotency safety; auditability | OPS-208 repo-side hardening binds status history to candidate digests, resets changed same-ID candidates to draft for re-review, and builds promotion approval trails only from current-digest history. | Live shared shadow candidate-store behavior, customer approval workflow proof, customer PEP no-bypass, and production readiness remain required. | Keep shadow persistence, approval-route, and promotion-draft tests green; re-review old approved candidates that lack digest-bound status history. |
| Policy Foundry readiness proof source | customer authority; proof integrity; tenant isolation; no overclaim | OPS-209 repo-side hardening makes `tenantBoundaryProven` explicit in the evaluator, rejects caller-supplied positive readiness proof query flags on hosted readiness/active-question routes, and derives customer approval only from a tenant-bound stored current candidate status whose digest matches the selected candidate. | Live customer approval workflow proof, live shared shadow candidate-store proof, customer PEP no-bypass, and production readiness remain required. | Keep readiness and hosted readiness route tests green; do not treat query flags as proof. |
| Shadow mutation idempotency matrix accuracy | replay and idempotency safety; auditability; no overclaim | OPS-210 repo-side hardening changes the hosted authorization matrix to `optional_header_plus_rate_limit`, matching runtime behavior where `Idempotency-Key` enables replay/conflict records when supplied and keyless shadow mutations are only route-rate-limited. | Live shared shadow idempotency proof remains required for header-bound retries; keyless retries have no service-level replay/conflict proof. | Keep hosted matrix and shadow route idempotency tests green; do not describe keyless shadow mutations as idempotent. |
| Degraded-mode approval provenance | customer authority; auditability; fail-closed boundary | OPS-189 repo-side hardening rejects body-supplied approval/authorizer/revoker fields on degraded-mode admin routes and derives route authority from the authenticated admin credential. | Live/operator proof is still needed for any independently authenticated dual-approval workflow. | Add a separate approval workflow only with signed or independently authenticated approval records; do not use request-body approver lists as authority. |
| Account recovery-code one-time claim | customer authority; replay and idempotency safety; auditability | OPS-190 repo-side hardening consumes recovery codes through a file-lock or PostgreSQL row-lock store operation before MFA login session issuance or MFA disable side effects. | Deployed account-route probes and shared auth-abuse-store behavior remain live-proof-only. | Keep account MFA replay and state-service boundary tests green before stronger account-security claims. |
| Live proof separation | runtime readiness; operational boundedness | `docs/audit/live-proof-register.md`; `scripts/check/check-ops-live-shadow-readiness.mjs`; OPS-SWEEP-01/02 live-shadow and PKI/TLS proof classifications in `docs/audit/finding-index.md`; OPS-SWEEP-07 stage-aware proof gate alignment; OPS-SWEEP-08 webhook provider proof gates; OPS-SWEEP-09 account callback/audit/idempotency proof gates; OPS-SWEEP-10 pipeline idempotency proof gate; OPS-SWEEP-11 shadow mutation audit/rate-limit/idempotency proof gates; OPS-SWEEP-12 release-route role enforcement proof gate; branch-protection proof gates for OPS-121/122; release decision log shared-store proof gate `ATTESTOR_RELEASE_DECISION_LOG_SHARED_STORE_PROOF`; policy-control mutation/approval shared-store proof gates `ATTESTOR_POLICY_MUTATION_AUDIT_CHAIN_PROOF` and `ATTESTOR_POLICY_ACTIVATION_APPROVAL_STORE_PROOF`; consequence retry-attempt shared-store proof gate `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF`; workload-binding extraction and token-exchange actor-proof live proof rows. | HTTPS, IAM, KMS, ClusterSecretStore, WAF, replay/introspection, workload-binding extraction, token-exchange actor proof source, customer PEP, branch protection signatures/reviews, KEDA Redis TLS, KEDA Prometheus auth, admin role-key/rate-limit behavior, release-route role enforcement, webhook provider endpoint/fake-signature proof, federated callback rate-limit proof, account mutation audit-chain proof, account mutation idempotency proof, shared auth-abuse-store proof, pipeline idempotency proof, shadow mutation audit-chain proof, shadow mutation idempotency proof, release decision log shared-store proof, policy mutation audit-chain shared-store proof, policy activation approval shared-store proof, and consequence retry-attempt shared-store proof. | Capture live proof artifacts without raw customer data; keep register entries aligned with the gate. |
| Release provenance | release provenance | SHA-pinned workflows, branch governance, stale branch remediation docs, PR-contract validation, supply-chain gates, CodeQL security-extended, evaluation artifact/SBOM attestation, and OPS-SWEEP-15 audit of 13 governance artifacts. Branch-governance now asserts GitHub `required_signatures` and required PR review settings on scheduled/manual/master-push governance runs. CONTRIBUTING documents workflow permission discipline for elevated write scopes, `.github/CODEOWNERS` owns that guidance, and OPS-125 now forbids placeholder per-surface team slugs until visible write-enabled GitHub teams plus membership policy exist. | Branch protection settings and unsigned/self-merge negative behavior remain live-proof-only until `LP-BRANCH-PROTECTION-SIGNATURES` and `LP-BRANCH-PROTECTION-REVIEWS` are captured. Per-surface CODEOWNER split remains blocked on operator team creation and membership policy. | Enable the GitHub settings and capture negative merge proof before stronger release-provenance claims; keep new write-scoped workflow permissions tied to documented justification and regression checks; split CODEOWNERS by surface only after real team evidence exists. |
| Production rehearsal artifact and approval boundary | data minimization and redaction; proof integrity; customer authority; no overclaim | OPS-211/212 repo-side hardening constrains production-promotion manifest artifacts to explicit evidence roots, records denied artifact metadata, requires independent go/no-go approval provenance, and prevents workflow dispatcher identity from satisfying the approval gate by itself. | Live protected environment reviewer configuration, signed approval artifacts, approval evidence retention, operator manifest custody, and production deployment proof remain outside repo-side closure. | Keep promotion-bundle and go/no-go tests green; capture protected-environment/signed approval proof before any target promotion claim. |
| Proof and signing authority | proof integrity; customer authority | Ed25519 verification, trusted fingerprint matching, strict Attestor-specific signing canonical JSON, `tests/signing-primitives.test.ts`, release/signing canonicalization parity tests, SPKI-DER fingerprints, PKI trust-chain verification, PKI-bound certificate verification, production-like keyless CA auto-generation fail-closed behavior, OPS-SWEEP-19 repo-proven release-kernel evidence packs, shared PostgreSQL release decision log bootstrap, deterministic-check observation-array resource budget, and OPS-SWEEP-21 repo-proven release-policy-control-plane policy bundles using DSSE payload type `application/vnd.in-toto+json`, in-toto Statement v1, DSSE envelopes, SHA-256 digests, and key fingerprint binding. Policy activation approvals now reject post-expiry decisions and use deterministic binary request ordering, while scope precedence locks environment, hierarchy, specificity, and ambiguity behavior. The primitive `src/signing/*` certificate/trust-chain layer remains Attestor-specific canonical JSON and is not itself a DSSE/in-toto signer. | External KMS/HSM runtime signing proof; live keyless CA configuration proof; release decision log shared-store proof in a customer-operated environment; policy mutation audit shared-store proof; policy activation approval shared-store proof; route/API parser budgets for newly external deterministic-check observation inputs. | Capture OPS-142 KMS runtime signing, `ATTESTOR_RELEASE_DECISION_LOG_SHARED_STORE_PROOF`, `ATTESTOR_POLICY_MUTATION_AUDIT_CHAIN_PROOF`, and `ATTESTOR_POLICY_ACTIVATION_APPROVAL_STORE_PROOF` before stronger limited-enforcement claims; keep external deterministic-check inputs bounded at the parser boundary. |
| Token and replay safety | replay and idempotency safety | DPoP (RFC 9449) proof shape, frozen default DPoP accepted-algorithm allowlist `[ES256, EdDSA]`, explicit caller-controlled allowlist expansion, `ath`, replay-key, and max-age checks; online verifier introspection with optional `consumeOnSuccess`; HTTP message signature presentation narrowed to Ed25519; workload binding via RFC 8705 `x5t#S256` and SPIFFE; required sender profiles now reject non-sender-constrained modes in the core verifier; release-token registration is insert-only for lifecycle finality; RFC 8693 token-exchange constants plus dedicated requested/subject token-type denial, audience/resource/scope narrowing, parent/source audience linkage, nested signed-parent `act` actor-chain evidence, trusted actor binding for `requireActor`, duplicate child-token ID denial, and raw actor-token fail-closed no-support; Envoy ext_authz route risk-class context binding with invalid-value fail-closed behavior; freshness/replay primitives; degraded-mode bounded defaults; account-admin, pipeline, async queue-claim, shadow mutation, and Policy Foundry persisted wizard-state idempotency evidence; OPS-SWEEP-20 maps this release-enforcement-plane evidence at spec, entry-point, export, and test-inventory depth; OPS-178 now defaults low-level offline verifier tenant binding to the request enforcement point tenant unless an explicit tenantless opt-out is supplied. | Multi-instance replay/introspection store proof; workload-binding extraction proof; token-exchange actor proof-source extraction proof; account/pipeline/shadow shared idempotency proof; raw external actor-token validation remains unsupported in this repo contract; live Envoy/Istio route-map deployment proof; customer PEP no-bypass proof; no claim that all 16,972 enforcement-plane lines are line-by-line behaviorally audited. | Run HA replay/outage tests, workload/actor extraction probes, and customer PEP no-bypass proof before stronger live enforcement claims. |
| Consequence admission boundary | fail-closed boundary; customer authority; replay and idempotency safety | OPS-SWEEP-22 maps the action-side consequence-admission substrate: explicit two-surface facade, four-decision admission model, required `narrow` constraints, retry-attempt binding, no-go-condition ledger with natural-language bypass reason codes, tamper-evident history with `rawPayloadStored: false`, presentation replay ledger atomic `setIfAbsent`, downstream contract typed `failClosed: true`, and customer-PEP runtime adoption with `productionReady: false` and `activatesEnforcement: false`. OPS-167/168 remediation now makes observedFeatures upstream/operator-derived evidence only at both docs and runtime contract level: caller-supplied positive readiness cannot satisfy generic `adapter-readiness` without a trusted `observedFeatureOrigins.adapterReady` marker, while restrictive signals can still hold, narrow, or block. Generic admissions now also run the untrusted-content authority guard against structured `authoritySources`, so untrusted content, tool output, retrieved content, and model summaries cannot authorize a consequence or promote themselves into trusted authority. Approval-backed authority now also runs the approval-provenance guard against structured `approvals`, so missing, untrusted, model-generated, or tool-output approval claims hold or fail closed. The generic path now also runs the no-go condition ledger against structured `noGoConditions` and no-go bypass signals, so active holds and hold-bypass language block before downstream execution while only digest/count evidence is surfaced. Generic admissions also run the scope-explosion guard when structured `scopeOwnerPolicyRef`, `requestedScope`, or `approvedScope` metadata is supplied, so requested-vs-approved scope expansion can narrow or block with digest-only constraints instead of trusting caller text. Generic admissions now run the tool-result poisoning guard when structured `toolResults` metadata is supplied, so untrusted or model-generated tool outputs cannot become authority or evidence without trusted source class, timestamp, integrity digest, evidence digest, and allowed evidence-class metadata; the response carries guard outcome, counts, and digest evidence instead of raw tool-result refs. Finance pipeline admission now reuses the authority, approval, and tool-result guards when structured finance metadata is provided, holding native finance `admit` / `narrow` at canonical `review` on guard review/block outcomes while carrying digest-only guard evidence. Generic admissions now run the stale authority/policy guard when structured `staleAuthorityPolicy` metadata is supplied, so policy mismatch, superseded policy, policy update after approval, expired approval, expired authority, no-go drift, or no-go reasons block while missing policy/freshness metadata holds for review with digest/count evidence only. Generic admissions now run the agentic supply-chain guard when structured `agenticSupplyChain` metadata is supplied, so unpinned source, missing integrity/provenance, unverified provenance, overbroad permissions, unreviewed generated adapters, unverified domain-pack boundaries, unsafe critical components, and untrusted publishers can hold or block with digest/count evidence only. Generic admissions now run the human-review fatigue guard when structured `humanReviewFatigue` metadata is supplied, so missing review-packet risk summaries, missing next safe step, review overload, unprioritized blockers, raw payload storage, or auto-enforce requests can hold or block with digest/count evidence only. Generic admissions now run the multi-agent delegation guard when structured `multiAgentDelegation` metadata is supplied, so missing identity, missing authority, missing scope, unapproved delegated scope, delegation cycles, self-approval, cross-tenant unscoped delegation, or over-depth chains can hold or block with digest/count evidence only. Generic admissions now run the decision-context drift binding when structured `decisionContextDrift` metadata is supplied, so missing required bound/current model, tool-schema, policy, or config context blocks while model/tool/policy/config/prompt/verifier/simulation drift, expiry, age excess, or required simulation refresh holds with digest/count evidence only. Retry-attempt shared-store proof remains repo-gated through `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF`. OPS-170 focused sub-sweep locks the central `index.ts` response, generic envelope, and retry-budget evidence surfaces without claiming full behavioral closure. OPS-171 family queue keeps policy-foundry, shadow, golden-refund, assurance/invariant, action-surface, customer-gate, and test assertion sub-sweeps backlog-tracked. | Full line-by-line behavioral audit of all 155 consequence-admission files is not claimed; generic policy/evidence refs are still reference-level contracts rather than semantic policy-store/evidence-store validation; trusted generic/finance `authoritySources`, `approvals`, `noGoConditions`, structured scope metadata, trusted `toolResults`, `staleAuthorityPolicy`, `agenticSupplyChain`, `humanReviewFatigue`, `multiAgentDelegation`, and `decisionContextDrift` metadata still depend on customer/operator/tool-adapter/policy-store/IdP/approval-workflow/source-control/package-registry/provenance-system/runtime-inventory/change-management/reviewer-workflow/agent-runtime evidence; signed-approval PKI verification and signed tool-result attestation verification are not exposed as generic route inputs in this slice; third-party code behavior, live runtime inventory, live reviewer behavior, live delegation authority, and production supply-chain provenance are not proven by repository metadata; shared retry-attempt ledger and Customer PEP no-bypass remain live-proof-only; OPS-171 queue does not prove family-level behavioral closure. | Capture `ATTESTOR_CONSEQUENCE_RETRY_ATTEMPT_LEDGER_PROOF`; Customer PEP no-bypass remains behind `LP-CUSTOMER-PEP-NO-BYPASS`; run queued family-specific sub-sweeps only when module-specific risk warrants it. |
| Service/API boundary | customer authority; tenant isolation; auditability | Edge contract, auth/MFA/session/federated auth sampled; OPS-SWEEP-06 maps the 32-route admin surface; OPS-SWEEP-08 maps and hardens the 3 inbound webhook signature surfaces; OPS-SWEEP-09 maps the 49-route account surface, adds federated callback rate limiting plus account-session mutation audit, and follow-up remediation closes account-admin mutation idempotency, strict JSON handling, shared local password-policy validation, and typed auth-attempt bucket vocabulary; OPS-SWEEP-10 maps the pipeline execution/async surface, adds pipeline `Idempotency-Key` replay protection plus deterministic BullMQ queue claims for idempotent async retries, closes pipeline strict JSON handling, error-detail redaction, runId nonce hygiene, and decomposes sync `/run` release-target/auto-filing stages into named route helpers; OPS-SWEEP-11 maps the 27-route shadow surface, adds shadow mutation audit plus direct HTTP route coverage, and route cleanup closes the shadow non-JSON 415 status mismatch, bounded shadow list pagination, bounded shadow problem details, tenant+route scoped shadow mutation 429 handling, and shadow POST/PATCH `Idempotency-Key` replay/conflict handling repo-side; OPS-SWEEP-12 maps the 31-route release-decision surface, adds credential-bound role enforcement to release-review and release-policy-control, separates policy-domain actor labels from admin authorization roles, and PR9 closes release-review HTML security headers plus release-policy-control typed error mappings and bounded list pagination; OPS-SWEEP-13 maps the 5-route onboarding/admission surface, verifies envelope tenant binding/plan-mode/agent-loop/protected-token/sender-confirmation/fail-closed shadow recording, and PR9/follow-up remediation closes hosted onboarding tenant assertions, hosted HTML security headers, onboarding/admission 415 content-type handling, generic admission problem-detail redaction, and Policy Foundry persisted wizard-state replay/conflict handling; B-033 route/token/proof/dashboard tenant-boundary tests now lock optional payload mismatch, protected token/introspection tenant binding, shadow dashboard fail-closed redaction, and downstream proof-chain mismatch blockers; OPS-SWEEP-14 maps the final 21-route verification, filing, public-site, and core surface, verifies PKI mandatory gate, release-token consume-on-success, path traversal defense, and public trust-root contract, and completes the `/api/v1/**` route-layer deep-audit chain; OPS-112/113 are repo-side remediated with minimal unauthenticated health/ready responses and a source-scoped `/api/v1/verify` rate limit before JSON/PKI work; OPS-114/117/118/119/120 add redacted structured verify logging, bounded verify/filing problem details, minimal startup diagnostics, and public connector catalog redaction; OPS-115/116 reuse shared public-site HTML security headers and no-store cache handling for public dynamic pages, committed evidence assets, and the legacy redirect; OPS-176/177 add server-side shadow activation handoff lookup for receipts and credential-derived admin/release actor attribution. | Live route probes, customer PEP integration, role-scoped admin key deployment, release-route role enforcement proof, legacy superuser-key rotation, admin/webhook/account callback rate-limit abuse proof, provider webhook endpoint proof, email webhook replay-store proof, account mutation audit-chain proof, account mutation idempotency proof, shared idempotency live proof, shadow mutation audit-chain proof, shadow mutation rate-limit/idempotency abuse proof, live shared-store/RLS tenant-boundary proof, health diagnostic split proof, and verify-route rate-limit proof. | Capture `LP-HEALTH-DIAGNOSTIC-SPLIT`, `LP-VERIFY-RATE-LIMIT`, `LP-SHADOW-MUTATION-RATE-LIMIT`, `LP-SHADOW-MUTATION-IDEMPOTENCY`, and `LP-ACCOUNT-MUTATION-IDEMPOTENCY`; keep live shared-store/RLS tenant proof gaps visible. |
| Ops and infrastructure | runtime readiness; operational boundedness | Ops Sweep 01/02/03/04/05/06/07/08 remediation, HA manifests, observability alerting/token hardening, PITR/Redis recovery hardening, collector securityContext, observability namespace NetworkPolicy/Pod Security Admission labels, admin-route role/rate-limit hardening, release-route role enforcement, AWS HTTPS/WAF example, KEDA TLS/auth examples, webhook provider proof gates, and stage-aware live proof register gates. | Cloud/IAM/KMS/network/storage observability proof, alert delivery, backend auth/storage, PITR/Redis recovery proof, CNI/Pod Security Admission enforcement proof, budget telemetry proof, admin role-key deployment proof, release-route role enforcement proof, admin/webhook rate-limit abuse proof, provider endpoint proof, AWS HTTPS/WAF proof, and KEDA scaler proof. | Continue ops sweep with remaining provider/live proof surfaces and capture live proof artifacts before stronger runtime claims. |
| PITR/Redis recovery | runtime readiness; replay and idempotency safety; operational boundedness | `ops/postgres/pitr/archive-wal.sh`, `ops/postgres/pitr/restore-wal.sh`, `ops/redis/redis-recovery.conf`, `docker-compose.dr.yml`, and OPS-SWEEP-04 remediation. | Offsite WAL object-store durability, base backup scheduling, restore drill, Redis ACL/network isolation, and secret rotation proof. | Capture live DR evidence before limited enforcement or stronger recovery claims. |
| Public demo safety | data minimization and redaction; no overclaim | Baseline demo path traversal and provider redaction gaps are repo-side remediated: provider/runtime-marker redaction patterns, false-positive guards, committed public artifact scan, generated proof/showcase/release-provenance artifact scan, Security Scan wiring, release-provenance pre-upload scan, and demo/showcase basedir constraints are locked by tests. | Public demo artifacts generated outside selected scanned roots, binary artifacts, and live/operator artifacts still need explicit context review; operator override paths are local-only and not public proof. | Keep `check:public-artifacts-redaction`, `test:production-readiness-secret-safe-output`, release-provenance workflow guards, and demo path boundary tests green before public demo / marketing claims. |

## Update Rule

When a PR changes a control claim:

1. update the relevant evidence doc,
2. update `finding-index.md` if a finding state changed,
3. update `live-proof-register.md` if live proof is still needed,
4. update this map if the external anchor or control claim changed,
5. state what the PR does not prove.
